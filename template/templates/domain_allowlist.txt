# claude_researcher Domain Allow List
#
# NOTE: this file is only relevant if your claude.ai network-egress setting
# uses a domain allow-list. If you enabled egress in an "allow all domains"
# mode, no allow-list applies — there is nothing to paste; keep this file
# only as a record of the domains the workflow expects.
#
# If your egress setting DOES use a domain allow-list (in claude.ai
# Settings > Capabilities, under the network-egress option): paste the
# non-comment lines below into it. This is a user-wide setting — one-time,
# applies across all your claude.ai chats, not configured per-Project.
#
# One domain per line. Strip the comment lines (starting with #) before
# pasting if the allow-list field does not tolerate them.

# --- Workflow-required (GitHub REST API + raw content fetch) ---
api.github.com
codeload.github.com
github.com
raw.githubusercontent.com

# --- Paper sources (PDF download for the add-paper skill) ---
# Note: the `www.` prefix on biorxiv/medrxiv is intentional. Those sites'
# canonical hostnames include `www.`, while arxiv.org and doi.org canonically
# don't. Match each site's canonical hostname; do not "normalize" them.
arxiv.org
doi.org
www.biorxiv.org
www.medrxiv.org
