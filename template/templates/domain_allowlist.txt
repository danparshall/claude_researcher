# claude_researcher Domain Allow List
#
# Paste the non-comment lines below into your claude.ai user Settings at:
#   Settings > Capabilities > Allow Network Egress > Domain Allow List
#
# This is a user-wide setting (one-time, applies across all your claude.ai
# chats — not configured per-Project). This file is a record of what was
# pasted during bootstrap, useful for re-use on a different machine.
#
# One domain per line. Strip the comment lines (starting with #) before
# pasting if claude.ai's allow-list field does not tolerate them.

# --- Workflow-required (GitHub REST API + raw content fetch) ---
api.github.com
codeload.github.com
github.com
raw.githubusercontent.com

# --- Paper sources (PDF download for the add-paper skill) ---
# Note: the `www.` prefix on biorxiv/medrxiv is intentional. Those sites'
# canonical hostnames include `www.`, while arxiv.org and doi.org canonically
# don't. Match each site's canonical hostname; do not "normalize" them.
arxiv.org
doi.org
www.biorxiv.org
www.medrxiv.org
