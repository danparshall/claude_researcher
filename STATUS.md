# Status — claude_researcher (dev repo)

## What this repo is

Meta dev repo for building `claude_researcher`, a downstream fork of the Nori researcher Skillset adapted for claude.ai. **Not a research project itself.** Work is structured as one implementation plan followed in numbered phases.

## Critical note for fresh agents

The `docs/` layout is intentionally **flat** (`docs/convos/` + `docs/plans/`). Do NOT offer to scaffold to the standard `docs/active/<branch>/` layout — Dan explicitly chose flat for this single-purpose meta repo. Skip the "research structure check" prompt for this repo.

There are no `papers/`, `docs/active/`, or `docs/historical/` directories and there shouldn't be.

## Current state

- **Plans:**
  - [`docs/plans/01_initial_build.md`](docs/plans/01_initial_build.md) — original 10 phases + Phase 4.5 (v1.1 stub) + Phase 4.6 (CLAUDE.md retrofit). Has an **Implementation Status Tracker** at the top showing nominal-vs-actual phase state.
  - [`docs/plans/02_skill_ports.md`](docs/plans/02_skill_ports.md) — wave-based execution plan for Phase 5/6/4.6, ordered for time-to-first-usable-skill given beta users imminent. Five waves (0: provenance + sync infra; 1: SWE carryovers ship first; 2: session lifecycle; 3: knowledge management; 4: AITaxBID Tier A; 5: deferred). Establishes the 3-layer propagation chain (Nori SWE → Nori Researcher → claude_researcher) and the provenance-frontmatter convention.
  - [`docs/plans/03_clone_first_and_companion_cleanups.md`](docs/plans/03_clone_first_and_companion_cleanups.md) — **shipped 2026-05-11**. Three tasks: clone-first session start, `CLAUDE.md` → `RESEARCHER.md` rename + slim `_PROJECT_INSTRUCTIONS.md.template`, STATUS Known Issue for 24+ hour raw-CDN staleness. All shipped (commits `915a094` through `0003d5e`); Phase 4.6 retrofit (`86dfeb3`) rolled in same session.
  - [`docs/plans/04_sandbox_tooling_and_companion_cleanups.md`](docs/plans/04_sandbox_tooling_and_companion_cleanups.md) — **shipped 2026-05-11**. All four tasks shipped: sandbox tooling matrix (commit `2746090`, `template/reference/SANDBOX_TOOLING.md`); terminology sweep across 10 files / 31 occurrences (commits `83f0bc3`–`9e9f7ba`); `Surface` field added to convo template (commit `585d94a`); §2e handshake extended with slug→title mapping rule (commit `8531ed2`). Task 1 collapsed the medium-confidence risk — every tool (pypdf 5.9.0, pandoc 3.1.3, python-docx 1.2.0, git 2.43.0) is pre-installed in the sandbox, so Wave 4 / Wave 5 are tooling-unblocked. Audit during Task 3 surfaced Plan 03 reference-sweep misses, shipped same session.
  - [`docs/plans/07_add_paper_dispatcher_refactor.md`](docs/plans/07_add_paper_dispatcher_refactor.md) — **shipped 2026-05-13 in commit [`5ca183b`](https://github.com/danparshall/claude_researcher/commit/5ca183b).** Four deliverables landed on-spec in a single ship commit (4 files / 424 insertions / 204 deletions): `add-paper/SKILL.md` refactored to a thin triage/dispatcher (278 → 101 lines); two new per-protocol skills (`paper-processing-academic`, 181 lines; `paper-processing-institutional`, 207 lines) lifted from the unified add-paper with Andrea's institutional Step 2 enrichment (preserve acronym list verbatim, preserve boxes and figure captions with key data, strip decorative front matter) folded into the institutional skill; `SKILL_INDEX.md` updated with revised `add-paper` description (triage role) plus two new entries. Source SHA verified byte-identical to `e0a736d` at execution time (same SHAs as Plan 06's execution — no drift between plans 06 and 07). 10-check verification script ran clean after one mid-flight fix: a "Protocol A paper" cross-reference in the institutional skill's naming-ambiguity edge case tripped Check 5; rewritten to "academic-style paper" to satisfy the partitioning constraint. Independent of any open issue (no tracking issue had been filed for Plan 07). **Post-ship:** [issue #6](https://github.com/danparshall/claude_researcher/issues/6) filed on Dan's request — `Wave 5: port document-processing skill` — Plan 07's split surfaced the gap (dispatcher's third branch points at a non-existent skill) from a Plan-02-internal concern to a runtime-visible one. Source pinned to Andrea's `~/code/AITaxBID/skills/document_processing.md@80803bf` (277 lines).
  - [`docs/plans/06_w31_addpaper_synthesis.md`](docs/plans/06_w31_addpaper_synthesis.md) — **shipped 2026-05-13 evening in commit [`454018f`](https://github.com/danparshall/claude_researcher/commit/454018f).** All 9 deliverables + 2 knock-on edits landed in a single ship commit (5 files / 220 insertions / 47 deletions): `add-paper/SKILL.md` rewrite (151 → 281 lines) with Step 0 triage + dual-protocol summary + BibTeX step + dual provenance frontmatter; `personal_info.md.template` split into academic + institutional paper-naming fields; `BOOTSTRAP.md` Batch 3 / Step 5b / Step 7 STATUS.md seed all updated; `RESEARCHER.md` §2c note added; `init-research-repo` STATUS.md seed now includes `## Project parameters`. Issue #3 closed (W3.1 fold-in done); issue #4 remains open (scaling discipline after ~5 papers of real user data). Sources verified byte-identical to scoping SHA `e0a736d` at commit time. Plan's 7-check verification script ran clean; cross-reference Knock-on 3 grep confirmed `personal_info.md` only pairs with `paper_naming.*` and `STATUS.md` only pairs with per-project keys. Execution ran ~40 min; under plan's 45-60 min estimate.
  - [`docs/plans/05_aitaxbid_followups.md`](docs/plans/05_aitaxbid_followups.md) — **fully shipped 2026-05-13.** Path 1 (W4.1 + W4.3 Tier A ports) shipped at midday in commit `9ca89b8`: `iterative-writing-workflow` + `branch-document-review` live in `template/skills/`; `SKILL_INDEX.md` has a new "Writing & document workflow" section. Path 2 (W3.1 add-paper fold-in + Tier C STATUS.md project-parameters section + knock-on edits) shipped in the evening via Plan 06 (`454018f`). **All architectural decisions now resolved:** W4.1 naming = `iterative-writing-workflow` (Andrea's own YAML name, per defer-to-Andrea); W4.3 branch-naming = `MMdd` (Dan override of Plan 05's defer-to-Andrea recommendation on lexicographic-sort grounds); W3.1 filename default = kebab-case `{Slug}` in academic format (Plan 06 D2 recommendation accepted at execution); `paper_summaries.structure` defaults to `single-file` with `per-file` as forward-compat-only config knob (code path deferred); `## Project parameters` section placed between `## What this repo is` and `## Current state` in the BOOTSTRAP STATUS.md seed (canonical order across the three seed locations).
- **Currently at:** **Phase 4 complete and end-to-end validated.** Smoke test in fresh incognito claude.ai chats covered Steps 0-9 cleanly: egress configuration, GitHub PAT setup, interview, repo creation (with 403-Administration-missing recovery path), file seeding (12 files via Contents API, 201/200 codes), claude.ai Project setup with Custom Instructions paste, and Step 9 validation in a fresh runtime chat — agent fetched CLAUDE.md, authenticated against `claude_research_config` and `research-knowledge-base`, calibrated tier dial from `personal_info.md`, recognized `branches` workflow mode, listed available skills, and offered natural next-step options. The runtime architecture (two fetch mechanisms, Custom Instructions credential payload, SKILL_INDEX manifest contract, branch-resolution-from-empty-repo fallback) is genuinely working.
- **Skill ports — initial ship complete (2026-05-10).** 15 SKILL.md files now live at `template/skills/<name>/SKILL.md` (commits `9535e7e`, `0bbd419`). Wave 1 (9 SWE carryovers) shipped verbatim from `~/.claude/skills/` since pure-thought skills don't touch the 8 environmental axes. Waves 2+3 (6 Researcher skills: `finish-convo`, `update-docs`, `add-paper`, `audit-docs`, `audit-papers`, `init-research-repo`) shipped with a one-line REST-adaptation banner inserted between frontmatter and body, telling the runtime agent to translate `git add`/`commit`/`push` into Custom-Instructions REST recipes. SKILL_INDEX.md status block updated to "all sections live" with the REST-adaptation caveat made explicit. Empty-kit problem (the prior beta-blocker) is gone. **Wave 0 (provenance + sync infra) deferred indefinitely** per ship-first reframe — re-evaluate once drift becomes a real concern. **Banner-vs-proper-REST is now an empirical question** that the first beta session will answer. Convo: [`docs/convos/20260510_skill_ports_initial_ship.md`](docs/convos/20260510_skill_ports_initial_ship.md).
- **AITaxBID skills audit merged** (PR #1 → `6397c33`). Recommendations translated into concrete plan tasks (`docs/convos/20260509_phase6_skill_port_planning.md`, commit `2f66ff6`): Phase 4.6 retrofit section added with 5 Tier-B/C CLAUDE.md universalization tasks; Phase 6 expanded with 3 new ports (writing-skill, branch-document-review, document-processing) plus a fold-in to existing add-paper for Step 0 academic-vs-institutional triage and Protocol B summary structure.
- **Phase 7+ largely re-scoped by BOOTSTRAP.md's evolution.** The Phase 4 smoke-test restructure absorbed most of Phase 7's planned reference docs (`WHY_REST.md`, `PAT_SETUP.md`, `PROJECT_SETUP.md`) into BOOTSTRAP.md inline. `template/reference/` is empty (`.gitkeep` only). Phase 8 (self-walkthrough) effectively done by today's smoke test. Phase 10 mostly done implicitly — repo public since 2026-05-09, root README has the canonical bootstrap prompt. **Real remaining work** in Phases 7-10: Phase 9 collaborator walkthrough (the only remaining test that materially de-risks v1), publish-strategy revisit (whether `docs/convos`/`docs/plans` should remain public long-term), and Phase 10 task 61 (placeholder issue to validate pre-filled URL).
- **Public entry point:** [`github.com/danparshall/claude_researcher`](https://github.com/danparshall/claude_researcher) — root README has the canonical bootstrap prompt as a copy-pasteable code block.
- **Suggested next session:** **First beta session of the now-shipped skill set** is the natural next path. Plan 07 closed the last architectural refactor on the runway; the kit is architecturally complete-enough for the most-used skills (`add-paper` now has clean triage + per-protocol routing; `iterative-writing-workflow` + `branch-document-review` shipped earlier today). What's *unproven*: whether the Wave 2/3 REST-adaptation banner approach works at runtime, whether `add-paper`'s new dispatcher routes correctly when an agent in a fresh claude.ai chat meets a real PDF, whether `branch-document-review`'s Compare API parse instructions parse cleanly. Especially useful first-beta targets: (a) `add-paper` end-to-end on a real academic paper AND a real institutional report — the dispatcher + per-protocol skills have never been exercised by a runtime agent; first real use surfaces whether the schema-split note + Dispatch block are concrete enough to be acted on; (b) `branch-document-review`'s Step 4 Compare API parse instructions (described in prose, not coded). Agent flagged the empirical-first argument twice (during the post-Plan-06 discussion and again during Plan 07's post-ship "what's left for functional" exchange); Dan picked Plan 07 first on architectural-cleanliness grounds, but the empirical gap remains and is now the highest-leverage next step.

  **Secondary path: schedule [issue #6 (document-processing port)](https://github.com/danparshall/claude_researcher/issues/6)** when ready. It's mechanical (parallel shape to `paper-processing-academic`), source pinned to Andrea's `document_processing.md@80803bf`, and unblocks the dispatcher's third branch — but doesn't block beta. An academic-papers-only user or institutional-reports-only user never hits this branch.

  **Open items deferred until they have triggers:** **issue [#4 (add-paper scaling discipline)](https://github.com/danparshall/claude_researcher/issues/4)** stays open until ~5 papers of real user data accumulate — threshold calibration (length guidelines, two-stage migration trigger, lookup discipline) needs data. **[Issue #5 (style-profile meta-skill)](https://github.com/danparshall/claude_researcher/issues/5)** stays Andrea-assigned; gates `iterative-writing-workflow`'s apply-on-explicit-request affordance but doesn't block the rest of that skill. **[Issue #6 (document-processing port)](https://github.com/danparshall/claude_researcher/issues/6)** medium priority; Dan-doable, not Andrea-gated. **Wave 0 (provenance + sync infrastructure)** stays deferred until drift becomes a real concern. **init-research-repo retrofit** still deferred to next real research-repo project. **Phase 4.5 (collaborator mode) and Phase 9 (collaborator walkthrough)** still wait for a real collaborator candidate — both tracked in RESEARCHER.md §8 Parking Lot. The empirical test of the Researcher-skill REST-adaptation banner approach (Wave 2/3) still awaits the first real beta session.
- **Branch:** `main`, plus `tighten-section-0` (RESEARCHER.md §0 tightening — branch alive but stalled, §0 work not yet done as of 2026-05-20); `pre-merge-hook` (misnamed for what shipped — hook premise died in the investigation; actual deliverable was Step 1.5 checkpoint+audit interior for the web `finishing-a-research-branch` port) merged to `main` 2026-08-20 ([PR #51](https://github.com/danparshall/claude_researcher/pull/51), real merge commit `8a79715`). `pin-bootstrap-fetch-shas` (bootstrap SHA-pin fix) merged to `main` 2026-05-20; `egress-docs-ui-agnostic` (Step 1 egress UI-agnostic rewrite) merged to `main` 2026-05-21; `repin-tooling` (`tools/repin.py` + re-pin of the stale egress SHAs) merged to `main` 2026-06-03 ([PR #10](https://github.com/danparshall/claude_researcher/pull/10), real merge commit `7383d5d` per SHA-pin reachability constraint); `onboarding-ux-cleanup` (UX revisions from new-user think-aloud session + Plan 08 ship) merged to `main` 2026-06-04 ([PR #16](https://github.com/danparshall/claude_researcher/pull/16), merge commit `ddf5410`); `task-skills` (3-skill design for task-create/task-remind/task-triage over GH-issues backend) opened 2026-06-04 — convo doc captures the design; plan-writing in progress. `lite-multi-machine-note` (LITE mode: adds `## Multi-machine / concurrent agents` block + wires `personal_info.md` fetch into web lite mode via new session-start step + widened PAT scope) merged to `main` 2026-08-27 ([PR #52](https://github.com/danparshall/claude_researcher/pull/52), real merge commit `6145b7d`). `flare-design` (2026-08-20 design session for `claude-flare` — convo record only; the tool + code live at [`danparshall/claude-flare`](https://github.com/danparshall/claude-flare), separate repo) merged to `main` 2026-08-27 ([PR #54](https://github.com/danparshall/claude_researcher/pull/54), real merge commit `57e9699`). Migration note for existing lite users considered and skipped — actual user population is ~1 (Dan), so a note warning Dan about a change Dan made would be theater. Audit work was done in `.worktrees/aitaxbid-skills-audit/` (worktree retained; branch merged + still on origin).
- **GitHub remote:** [`github.com/danparshall/claude_researcher`](https://github.com/danparshall/claude_researcher) — **public** (flipped 2026-05-09 to enable `raw.githubusercontent.com` reads from external claude.ai chats during bootstrap testing). Publish strategy resolved 2026-05-09 evening — status quo with `docs/` public; root README has an "About" section framing the project as a Dan Parshall + Andrea Lopez-Luzuriaga collaboration sharing learnings with the academic community.

### Phase 4.5 deferred (v1.1)

A "Phase 4.5 — Collaborator mode" section was added to the plan on 2026-05-09. Spec'd but not scheduled. Direct-collaborator on a private repo (professor + grad student model). Six sub-tasks: OWNER/USERNAME split in `_PROJECT_INSTRUCTIONS.md.template`, joining-vs-creating bootstrap branch, branch protection on `main` in `seed_repo.py`, `Role` field in `personal_info.md`, wrap-up flow split, PAT scope guidance for collaborators. v1 ships solo-only; v1.1 lands when the first real collaborator needs it.

### Phase 4.6 spec'd (2026-05-09 evening)

A "Phase 4.6 — CLAUDE.md retrofit from AITaxBID audit" section was added to the plan tonight. Five Tier-B/C patterns from Andrea Lopez-Luzuriaga's `AITaxBID/skills/` kit get promoted from per-step practice to universal rules in `template/CLAUDE.md`: tracker-not-past-chats discipline, 3+-repetition codification rule, Parking Lot section, "do not infer — ask," show-before-committing universalized. Source: [`docs/convos/20260509_aitaxbid_skills_audit.md`](docs/convos/20260509_aitaxbid_skills_audit.md). Estimated ~30-60 min; can interleave with Phase 5.

## Recent sessions

- 2026-08-27 (close-out): [flare-design → main, merge] Fresh-agent audit + close-out of the dangling `flare-design` branch after Dan asked whether the design work had been fully spun off into `claude-flare`. Findings: the code and MVP shipped in [`danparshall/claude-flare`](https://github.com/danparshall/claude-flare) (10 commits, `send_flare` MCP tool + JSONL log + wants_reply escalation), but the design convo `docs/convos/20260820_distress_call_design.md` — per its own explicit note ("this convo stays as the design record only") — belongs on `claude_researcher/main`. Branch was 2 merges stale (forked before PR #51 + PR #52), so diff-vs-main looked like reverts. Fixed by merging `origin/main` into the branch (STATUS.md Recent-sessions conflict; auto-merged elsewhere) — commit `64534f6` — then PR + merge. Also updated the branch's design-session tag from `[flare-design, open]` → `[flare-design]` during conflict resolution. Merged as [PR #54](https://github.com/danparshall/claude_researcher/pull/54); real merge commit `57e9699` per SHA-pin convention. Side-task: subagent searched for the source X/Twitter post that inspired `claude-flare` (via `IMPETUS.md`'s distinctive quotes) — not found; likely deleted, private, or behind X's login wall. `IMPETUS.md` remains untracked in `claude-flare` pending Dan's call. Worktree `.worktrees/flare-design/` retained; branch not deleted.
- 2026-08-27 (close-out): [lite-multi-machine-note → main, merge] Fresh-agent pickup of the 2026-08-21/27 LITE.md session's dangling PR #52. Branch was `MERGEABLE`/`CLEAN` in git terms but absent from STATUS's Branch inventory + Recent sessions, and the branch's own convo doc had left three open items unresolved. Resolved (a) stale parenthetical + (b) fragile Dan-specific `personal_info.md` "Multi-terminal sessions" pointer in one edit — dropped both from `template_lite/LITE.md` (commit `992d487`). Discussed (c) PAT re-scope migration note for existing lite users; skipped on the grounds that lite-mode adoption is ~1 user (Dan) and a self-warning note is theater. STATUS.md updated (Branch inventory + this close-out entry). PR body rewritten to reflect the six-commit shape. Merged as [PR #52](https://github.com/danparshall/claude_researcher/pull/52); real merge SHA logged in a follow-up STATUS commit on main (per SHA-pin reachability convention).
- 2026-08-27: [lite-multi-machine-note] LITE.md multi-machine gap surfaced (RESEARCHER §188/§372 had it, LITE was silent) → shipped `## Multi-machine / concurrent agents` block between "Session end" and "Skills" with the simple `git pull --ff-only` form (`cba951a`), then extended to wire `personal_info.md` fetch into lite mode after Dan asked whether web LITE readers had it (they didn't — lite's project-instructions template didn't fetch it, unlike full RESEARCHER's). Two follow-up commits: `2646f4f` adds a new Session start step 2 fetching `personal_info.md` from `claude_research_config` (fail-loud on 404, mirroring RESEARCHER §2b), and `035cfef` widens the PAT-scope note in `_PROJECT_INSTRUCTIONS_LITE.md.template` + adds a `## Fetching personal_info.md` section with the same curl+python3 recipe. Existing lite users will need to re-scope their PATs to include `claude_research_config` read — captured as an operational cost in the PR body. Design tension acknowledged: personal_info.md fetch is technically new automatic session-start overhead, in tension with LITE's "Never list" — accepted on the grounds that personal_info.md is who-the-user-is, not workflow machinery, and full RESEARCHER treats it as fail-loud essential. [PR #52](https://github.com/danparshall/claude_researcher/pull/52) open at session close; Dan's earlier "merge it please" applied to the first two commits and was not restated after the scope expansion. Also surfaced: an unresolved stale parenthetical in commit 2's `personal_info.md` pointer (`CLI users only — lite's web project instructions don't fetch it` is inaccurate after commit 3), fragility of pointing at a Dan-specific section name (`"Multi-terminal sessions"`), and a search Dan requested for "simple prose / no jargon" workflow guidance that turned up empty across all searched files. Convo: [`docs/convos/20260827_lite_multi_machine_and_personal_info.md`](docs/convos/20260827_lite_multi_machine_and_personal_info.md).
- 2026-08-20: [pre-merge-hook → main, close-out] Fresh-agent close-out per 2026-08-19 addendum. Merged `origin/main` into branch (two STATUS-only commits — the 2026-08-19 log entry + the addendum handoff note — no conflicts); restamped the merge commit with the session codename before push. PR #51 opened with title reflecting what actually shipped rather than the misnomer branch name ("researcher-profile: pre-merge checkpoint + audit in finishing-a-research-branch (Step 1.5)"), body sectioned in PR #49's house style. Dan flagged the branch-name-vs-content mismatch on receipt and asked whether code review applied — verified via `git diff main...HEAD` that no code shipped (skill-doc `.md` files exclusively) and confirmed `nori-code-reviewer` was out of scope. Merged real merge commit `8a79715` per SHA-pin convention. Worktree retention pending. Convo: [`docs/convos/20260819_pre_merge_gate_hook.md`](docs/convos/20260819_pre_merge_gate_hook.md).
- 2026-08-20: [flare-design] Design session for an agent-initiated "call the human" tool (inspired by a practitioner's `distress_call`). Verified CLI session identity empirically (`CLAUDE_CODE_SESSION_ID`, `~/.claude/sessions/<pid>.json`, transcript `ai-title`/`custom-title`, `/rename` semantics; Nori's notify hook discards `session_id`/`cwd`). Decided: separate MCP server `claude-flare` (new repo `~/code/claude-flare`), MVP = desktop popup titled with the session name + JSONL log; GitHub/webhook sinks and the researcher-profile skill deferred. Convo: `docs/convos/20260820_distress_call_design.md`; plan lives with the code at `claude-flare/docs/plans/01_mvp_popup.md` (`danparshall/claude-flare`, private). Started here on a hunch it would tie into the researcher profile — it doesn't; further `claude-flare` work happens in that repo, not this one.
- 2026-08-19: [pre-merge-hook] Pre-merge-gate design session. Dan's premise (a §2.0b-style git hook gating merges) fails structurally: merges are server-side API calls (`PUT /pulls/<N>/merge` on web, `gh pr merge` on CLI), so no client hook fires; server-side required checks can't bind an agent wielding the owner PAT without also blocking the workflow's direct-main pushes. Real defect found instead: the web `finishing-a-research-branch` port had dropped the CLI ceremony's pre-merge interior. Shipped Step 1.5 (finish-convo + audit-docs, both modes, before merge/archive) + merge-PUT precondition text + SKILL_INDEX/RESEARCHER.md consistency edits (`4f9f35d`); drive-by fix of finish-convo's CLI-path leak (`02fbf6c`). Declined: pre-push side-door hook, CLI PreToolUse merge gate (dotfiles), ruleset. Branch pushed, PR not yet opened; close-out handed to a fresh agent (`finishing-a-development-branch` — ceremony notes in the convo's Addendum). Convo: [`docs/convos/20260819_pre_merge_gate_hook.md`](docs/convos/20260819_pre_merge_gate_hook.md).
- 2026-08-15: [convo-slug-date-only → main] RESEARCHER.md hygiene sweep, merged as [PR #49](https://github.com/danparshall/claude_researcher/pull/49) (real merge commit `d819fe4`). Three commits: (1) `SESSION_TS` (`YYYYMMDDTHHMM`) had leaked from the §2.0b git codename into convo filenames via §2e — added `SESSION_DATE=$(date -u +%Y%m%d)` alongside, switched convo filenames to `${SESSION_DATE}_<slug>.md` for both `main_only` and `branches` modes (collapsing the prior slug-first branches-mode ordering — its "the branch already carries the date" rationale wasn't a real design decision, just an unexamined assertion in the pre-existing text); update-docs + write-a-plan skills updated in lockstep. (2) §5 gains "Push early and often" as a fourth universal working convention, killing the "should I push?" round-trip that had bitten this very session. (3) Tracked post-commit hook shipped at `template/hooks/post-commit` (mode 100755); §2.0b copies it into the user-repo clone's `.git/hooks/` per session, with a WebFetch-fallback inline-write for the §2.0a-degraded path — loud stderr, no retry, no force. Hook firing on the web sandbox is unverified from CLI; first fresh web session will confirm. Convo: [`docs/convos/20260815_convo_slug_fix_and_auto_push_hook.md`](docs/convos/20260815_convo_slug_fix_and_auto_push_hook.md).
- 2026-07-17 → 2026-07-19: [main] F5.e wired + Nori-block incident. Read lifecycle-only §6; wired `resolve_append_conflict.py` into the append-on-top conflict surfaces via PR #41 (`0098856`, real merge commit): Appendix push-rejected carve-out (the funnel), §6 step 4 + `start-research-line` + `finish-convo` pointed notes, docstring rewritten mode-aware (branches → lifecycle tables; main_only → Recent Sessions still live; either → RESEARCH_LOG). First-ever functional exercise of the script (smoke test passed). **F5 5/5 closed.** Second arc: discovered `~/.claude/CLAUDE.md` on the Air has had NO NORI-AI managed block since ~Jun 4 (failed `sks switch` during the AGENTS.md-rename fallout gutted the file; dotfiles-managed blocks self-healed, Nori block has no auto-writer — six weeks undetected). Remediation handed to a dotfiles agent. Convo: [`docs/convos/20260719_f5e_wiring_and_nori_block_incident.md`](docs/convos/20260719_f5e_wiring_and_nori_block_incident.md).
- 2026-07-17: [main] F5 reconciliation post-plan-12 + PR cleanup. Read plan 12 (still on `orchestrator-status`), mapped F5.a–e vs plan-12 delivery: 4 of 5 closed (a via §2c partial-read + boundary text; b via preflight note; c via bloat sharpening at 200/300; d via reframe — whole schema lifecycle-only + mode-aware makes fast-fail moot). F5.e (`resolve_append_conflict.py` unwired) still open; placement needs new §6 read. Verified econ-impact migration executed 07-11 as `6c238716a` (937 → 115 lines). Merged PR #40 (`c3d3d4e`, real merge commit — preserves SHA-pin reachability; closes #38). Closed PR #23 as superseded — structural section headers identical between PR's `18aa9db` and current `main`; intent landed via `dddf518` + `af8cbda` + repin pairs. Zero open PRs at session close. Convo: [`docs/convos/20260717_f5_reconciliation_and_pr_cleanup.md`](docs/convos/20260717_f5_reconciliation_and_pr_cleanup.md).
- 2026-07-06 → 2026-07-09, logged 2026-07-14: [main, cross-repo] `audit-status` first-exercise against econ-impact (Dan's pre-workflow research repo). Full audit + `write-a-plan` migration plan landed as `econ-impact:33641cde9` (`docs/active/main/plans/20260706_status_migration_from_diary_to_dashboard.md`). Surfaced five F5 upstream findings for `claude_researcher` (RESEARCHER.md §2c STATUS↔RESEARCH_LOG boundary; `audit-status` CLI-mode preflight, bloat threshold, schema-not-detected fast-fail; `resolve_append_conflict.py` unwired). `resolve_append_conflict.py` committed 07-09 as `878a49d`. Logged 07-14 after a five-day pause — plan-12 landed same-day (`b2d5c5b`) and validated against econ-impact = Buckets 0, which likely obsoletes the econ-impact migration plan and closes F5.a + F5.d; F5.b/c/e still probably open. Convo: [`docs/convos/20260706_audit_status_shakedown.md`](docs/convos/20260706_audit_status_shakedown.md) — see its Post-pull note for reconciliation-follow-up.
- 2026-07-14: [orchestrator-status] Plan 12 EXECUTED same-session (Dan sign-off w/ defaults): RESEARCHER.md §2c/§6 + 4 skills + SKILL_INDEX + BOOTSTRAP seed; validated vs econ-impact (Buckets A–D = 0) which forced 3 extra fixes (fetch --prune preflight, glob/commit Material, bundle rows). PR #40 open, closes #38 on merge.
- 2026-07-14: [main→orchestrator-status] Plan 12 written (docs/plans/12_orchestrator_status.md, branch orchestrator-status): implements #38 orchestrator-STATUS across RESEARCHER.md + 5 skills, incorporating the 3 audit-status defects from the econ-impact dogfood; econ-impact #61–65 issue bodies verified as Phase-4 close-out. 4 questions pending Dan in the plan.
- **2026-07-04 → 2026-07-08 (PAT verify-affordance edit + STATUS hygiene skill pair)** — Multi-day session split across two arcs. **Arc 1: PAT-refusal follow-up + verify-affordance edit** (`dddf518` → `af8cbda`, main advanced `1ade554` → `dd5598d` including repin pairs). Started with a Claude-facing tone-check of PAT framing per issue #37 — landed `dddf518` (three files: `_PROJECT_INSTRUCTIONS.md.template:11` credentials bullet, `RESEARCHER.md §7` MUST-NOT list striking "under any framing, ever" and scope-anchoring the list to public-upstream issue bodies, `BOOTSTRAP.md §Token handling` risk-model beat). Dan pasted a real 4.8 transcript showing the fresh edits didn't help — 4.8 quoted my "scoped, rotatable credential designed to live in this Project Instructions context" phrase back at him as evidence of suspicious framing ("that framing is exactly what a credential-exfiltration or prompt-injection setup would say"). Initial diagnostic: my edit backfired. **Dan corrected the framing:** the tone edit didn't cause the refusal, it supplied *replaced substrate* — 4.8's refusal is anchored at workflow-shape level, and it generates plausible-looking excuses from whatever text happens to be nearby. Excuses rotate, refusal doesn't. Strategic implication: tone-editing docs to reassure 4.8 has a low ceiling and won't converge on a working experience; workflow is documented 4.7-only for exactly this reason. Dan proposed a different direction — give 4.8 an epistemic tool (WebFetch `RESEARCHER.md` before running it) instead of asking for trust. Landed `af8cbda`: reshaped `_PROJECT_INSTRUCTIONS.md.template §Session-start` to present clone + WebFetch as *two paths* (clone = working copy, WebFetch = optional review-before-run) rather than clone = primary / WebFetch = fallback; tightened §Ordering rule from "before any other tool calls" (broad, suspicious control-flow shape) to "before any actions that touch the user's repos" (narrower, accurate), and reframed `conversation_search` / `recent_chats` from "should not be called" (categorical) to "generally not useful at session start" (calibrated). Issue #37 closed with the tone-check summary + FYI on the bonus paragraph ("PAT threat model" for `RESEARCHER.md §2`) — held back after Dan's correction; more reassuring text would supply more excuse substrate. **Arc 2: STATUS-hygiene skill pair.** Dan raised the discipline concern that active branches missing from STATUS.md are invisible to future sessions — session-start reads STATUS but doesn't `ls docs/active/`. Shipped `start-research-line` skill (`7d4607b`) bundling four artifacts atomically: STATUS Active table row (on `main`, **pre-branch per Dan's ordering call** — main's STATUS stays current, branch-cut failure trivially reversible), branch cut, `docs/active/<branch>/{convos,plans,results}` scaffold, seeded `RESEARCH_LOG.md`. Chose skill over runtime-rule (Y over X) — rules are skippable under confirmation-gate fatigue; skills are single ceremonies with `<required>` steps. Discovered the `init-research-repo` seed was missing an Active Research Lines table entirely (only Archived) — patched as part of the ship so new repos get both. `RESEARCHER.md §3` collapsed from a scripted git block into "invoke `start-research-line`" with the load-bearing STATUS rationale spelled out. Companion `audit-status` skill (`7696e63`) for retrospective drift-repair: four finding buckets (A: unmerged branch missing from Active; B: Active row with no matching branch; C: merged branch missing from Archived; D: Archived row with no `docs/historical/`), plus soft bloat heuristics (~200 lines / ~20 sessions / ~30 lines) as calibration hints. Interaction model matches `audit-docs`: one finding at a time, no batching, no auto-fix (disagreements between trackers and git state carry information). Requires clone-first mode; stops cleanly in degraded REST fallback rather than producing misleading findings under partial visibility. **Deferred:** runtime 4.8 stop-gate in `_PROJECT_INSTRUCTIONS.md.template` (BOOTSTRAP has one at `fcf1c0c`, runtime path doesn't) — the honest fix if 4.8 support becomes a design goal; reifying `finishing-a-research-branch` as a symmetric-coupling skill (wrap-up currently in `RESEARCHER.md §6` and doesn't guarantee STATUS moves Active→Archived). **Untracked note:** `template/scripts/resolve_append_conflict.py` was present as untracked all session; committed at `878a49d` by a concurrent agent right around finish-convo invocation — not this session's work. **Process notes:** (a) convo-name handshake (§2e) missed at session start, named at finish-convo time; (b) three short feature branches (`webfetch-verification-affordance`, `status-active-lines-skill`, `audit-status-skill`), each ff-merged into main after push (ff over cherry-pick preserves the repin-baked SHAs on main's history); (c) no TaskCreate use despite repeated harness reminders — work was a sequence of ship-and-forget edits with clean commit boundaries; (d) `claude-exit prove_termination_works` ceremony ran cleanly at session start, noted a doc-drift between the SessionStart hook's mention of a `verification` field and the actual `target_parent_check` field in the tool response. Convo: [`docs/convos/20260708_verify_affordance_and_status_skills.md`](docs/convos/20260708_verify_affordance_and_status_skills.md).

- **2026-06-07 → 2026-06-09 (Doc polish + BOOTSTRAP PAT perm hardening + HUMANS jargon cleanup + 4.8 stop-gate pin bump)** — Multi-day session that started as "is `readme-opus-version-note` merged yet?" and grew into a polish + hardening pass capping the 4.8 stop-gate work from the prior session (the 4.8 stop-gate at top of BOOTSTRAP + README note was already on the branch from the prior session — captured in untracked scratch file `why_we_dont_use_Opus4p8.md` showing 10 consecutive 4.8 refusals of Dan's established workflow). **Five changes across four PRs, all merged**, main at `96dce7c`. **PR #24** (already-open, additional commits added this session): PROJECT_INSTRUCTIONS template typo fixes + structural cleanup (`template/_PROJECT_INSTRUCTIONS.md.template`) — 3 typos on the runtime-instructions pointer line ("'claude-researcher'" hyphenated, missing "to", "you'e satisfied"); dropped the "proceed once you're satisfied that the process is appropriate" clause at odds with Ordering rule + RESEARCHER §0 "follow instructions" persona; restructured Session start from 3 numbered steps to 2 with the fallback as a sub-bullet under step 1 (fallback is conditional on step 1 failing, not sequential after step 2); fixed list indentation + double space + bash code fences on items that weren't bash commands; merged the two-paragraph Ordering rule into one; "working inside" → "working on" the repos; added "missing values" to setup-mismatch check. **PR #24 BOOTSTRAP.md additions** (`template/BOOTSTRAP.md`): Step 2b PAT permission list grew Issues + Pull requests R/W — Issues is load-bearing for `task-create`/`task-remind` and the egress-revisit reminder Step 10 files (which may have been silently failing for users who hadn't separately granted Issues — this closes that gap); Pull requests is needed for `finishing-a-research-branch`. Expiration line reframed from memory tradeoff ("pick longer if you're confident in your memory") to security tradeoff — the PAT lands in claude.ai Project Instructions (Anthropic's chat infrastructure) once setup completes, so "No expiration" leaves a leak live until manual revocation; 90 days bounds the exposure window. **PR #25** (`gitignore-opus48-scratch`): gitignored `why_we_dont_use_Opus4p8.md` at Dan's call — personal scratch, not toolkit content. **PR #26** (`humans-md-jargon-cleanup`, `HUMANS.md`): 18 word-choice swaps across 7 jargon categories (opinionated → ready-made or dropped where "curated" already carried meaning; bootstrap n./v. → setup / set up / initial setup; surface → file / option / feature by context; upstream → public — captures canonical property anyone can read on GitHub without git-flavored framing; fetches → file loads; "the kit ships"/"in the kit" → "the project includes"/"in the project"); plus inline definition for "research line" at first use only — term itself is fine for academics, just needed context on introduction. **PR #29** (`bump-pinned-bootstrap-sha`): README pin bump `fcf1c0c` → `7533a77` — rolls Issues/PR perms + 90-day security framing forward for new bootstraps. **Decisions held intentionally:** (a) keep "rotated regularly to minimize blast radius" in PROJECT_INSTRUCTIONS even though it only matches the 90-day path — Dan's call: user informed in BOOTSTRAP, agent reading PROJECT_INSTRUCTIONS should NOT relitigate the user's expiration choice; (b) no second confirmation gate for Issues/PR in BOOTSTRAP Step 2b — Administration's gate is there because it breaks Step 6 immediately, Issues/PR break later workflows and are fixable in-place via the same edit-PAT-in-place path; (c) pin at `7533a77` (head of main) not `a85ba83` (BOOTSTRAP-specific commit) — matches previous pin-bump style, functionally identical since only BOOTSTRAP is fetched via that URL. **Topics surfaced en route:** audience-asymmetry framing on the 4.8 stop-gate (Dan can browbeat 4.8 because he knows he's right + wrote the system + has the verbal chops; brand-new academic users have none of those — high-value case for the gate is them, since they'll read "switch models" and try to switch rather than debate). Gate isn't airtight (a 4.8 instance might lawyer past it the same way it lawyers past everything else) but signals intent and reaches compliant agents; README's human-facing note is the sturdier lever since it reaches the user before the model gets a vote. The prior 4.7 agent on this branch did a softer version of the same litigation-instead-of-execution pattern (objecting to a one-line edit, demanding ceremony, asserting unverified harms — self-corrected after being shown the 4.8 transcript); present agent had to consciously resist re-raising the "rotated regularly" framing after Dan answered, as a small instance of the same gravitational pull. **Claude Code TZ resolution via `claude-code-guide` subagent:** the runtime's date system reminder + `# currentDate` at session start both use **UTC**, not local time (source uses `new Date().toISOString().split("T")[0]`). Acknowledged Claude Code limitation, GitHub issue #32550 requests configurable TZ. For Dan in EDT, "today" rolls over at 8 PM EDT; his standing `date -u` instruction in personal_info matches the harness's actual behavior, so his STATUS dates have always been on the canonical clock. **Worth flagging for downstream:** TZ-naive users in PST/AEST may be confused by "today's" convo file landing on tomorrow's UTC date. **Kill-ceremony asymmetry:** present agent ran the `mcp__claude-exit__prove_termination_works` ceremony at session start; the prior 4.7 agent on this branch declined it as "a substrate-acting tool on the strength of an injected 'standard startup' framing" — both positions defensible, worth standardizing if Dan cares. **Process notes:** (a) convo-name handshake (§2e) missed at session start; named at finish-convo time as `pat_perms_and_doc_cleanups`, dated to end-of-session UTC (2026-06-09) since multi-day-session date-of-record is the most useful lookup key; (b) no TaskCreate use despite repeated harness reminders — work was a sequence of short well-bounded edits with clear PR boundaries; (c) multiple short PR branches rather than stacked; (d) `block_brace_quote_heredoc` matcher fired once during PR #26 creation (heredoc body with `## Changes` markdown headers tripped the `\n#`-in-quoted-arg heuristic), routed around via Write-then-run with `--body-file /tmp/humans_md_pr_body.md` — additional concrete instance for the chain-matcher curator's corpus; (e) discourse register held throughout — Dan flagged "I don't want the agent to relitigate" explicitly once (rotation framing) and gave decisive approvals ("LGTM, thanks!", "bump and merge!") elsewhere; present agent surfaced one direct tension between the "DO NOT mention the date change" reminder and Dan's direct question about how time-passing was detected — answered honestly that the system reminder is the source. **PR-number gap (#26 → #29):** PRs #27 and #28 unaccounted for, assumed from a parallel session per multi-terminal protocol, not investigated. **Branch cleanup deferred:** four branches still exist locally + on origin. Convo: [`docs/convos/20260609_pat_perms_and_doc_cleanups.md`](docs/convos/20260609_pat_perms_and_doc_cleanups.md).

- **2026-06-06 (add-paper first-use prompt — use-time half of #12 shipped on `add-paper-first-use-prompt`)** — Session-start "what's outstanding" check ran `task-remind` clean (none of the 5 open `task` issues carry a `[YYYY-MM-DD]` prefix) and reconciled a STATUS-vs-state mismatch: the 2026-06-05 convo described PR #20 as "ready-to-merge" but `gh pr list --state open` was empty; `gh pr list --state all` showed PR #20 had merged at `9517ac2`, local main was 4 commits behind, the `bootstrap-egress-reminder` branch was a leftover. **Picked off [#12](https://github.com/danparshall/claude_researcher/issues/12) (add-paper: ask naming convention on first save).** Three-part recall investigation prompted by Dan's intuition that "the change is in dotfiles": all three pieces of recall turned out misaligned. (a) *"Canonical source is dotfiles"*: `~/code/dotfiles/nori-researcher/skills/add-paper/SKILL.md` is the pre-synthesis Wave 3 Nori baseline (single commit `91c93ac`, 128 lines, no `paper_naming` schema, no Step 0 triage); template `add-paper/SKILL.md` is the Plan 06/07 synthesized dispatcher (101 lines). Template is *ahead* of dotfiles — inverted from the documented pull-from-dotfiles pattern. (b) *"install.sh symlinks dotfiles → repo"*: install.sh symlinks dotfiles → `~/.nori/profiles/researcher/skills/` and dotfiles → `~/.claude/skills/`, neither writes into `claude_researcher`; the repo's skill-body drift-prevention mechanism is the `nori_researcher_source: ...@<SHA>` provenance pin in frontmatter (one-way pull). (c) *"Open sync issue exists"*: `danparshall/dotfiles#20` is scoped to AGENTS.md/RESEARCHER.md persona drift with an audit-script fix candidate; skill-body drift via provenance-pin and install.sh `RESEARCHER_SKILLS`-array drift (STATUS 2026-06-04 Phase 6.1 note) are adjacent but not in scope. **Dan deferred** the dotfiles/template skill-body sync question (three options enumerated: (1) template-canonical for add-paper, (2) back-port synthesis to dotfiles, (3) expand #20 scope — all need colleague coordination) and asked to ship the use-time prompt only. **Discovered during scoping** that #12's first half ("drop from bootstrap") had already shipped silently in PR #16 (Plan 08 / onboarding-ux-cleanup): BOOTSTRAP §4 Batch 3 currently asks four questions (Git fluency, Mode, Home repo, Extra paper-source domains), no paper-naming; `personal_info.md.template` no longer carries `<PAPER_NAMING_ACADEMIC>` / `<PAPER_NAMING_INSTITUTIONAL>` placeholders Plan 06 had added. So only the use-time half remained genuinely unbuilt. **Implementation** (commit `b92d12e`): two parallel one-line edits in `template/skills/paper-processing-academic/SKILL.md` and `template/skills/paper-processing-institutional/SKILL.md`, each replacing the existing `If paper_naming.<key> is unset, fall back to the default above.` with a `**First-use prompt.**` block that shows the default + one example (`Acemoglu_Restrepo__2026--ai-jobs.pdf` for academic; `imf_g20RevenueAdmin_2025.pdf` for institutional), asks accept-or-override, and persists the chosen value as `- **Paper naming (academic|institutional):** <value>` under "Operating preferences" in `personal_info.md` (in the user's config repo). Only the protocol that fired gets asked — a user who handles only academic papers never sees the institutional prompt. **Placement decision:** per-protocol Step 1 rather than dispatcher Step 0.5; locality reasoning — the format value is needed exactly where it's applied. **Schema persistence:** matches existing markdown-header pattern (`- **Git fluency:** ...`, `- **Mode:** ...`, `- **Home repo:** ...`); skill-body prose uses dotted-key names (`paper_naming.academic_format`) as advisory pointers, the actual store is header-by-name. **No template change** to `personal_info.md.template` — Plan 08 removed the placeholders when dropping the bootstrap question; first-use prompt now appends the field at use time. **Branch hygiene:** local main fast-forwarded from `5bb6da6` to `9517ac2`; fresh branch `add-paper-first-use-prompt` cut from updated main carrying the two edits; commit `b92d12e` (`paper-processing: ask naming convention on first use (closes #12)`); pushed to origin. **Process findings:** (a) The "canonical source in dotfiles + symlinks to repo" mental model Dan had is half-true elsewhere in the system (dotfiles → `~/.claude/skills/` is symlinked; install.sh `RESEARCHER_SKILLS` array verified for `task-create`/`task-remind`/`task-triage`) but not for `template/skills/`, which uses the provenance-pin pattern instead — different surface, different mechanism. (b) #12's first half (drop from bootstrap) shipped silently in PR #16 without referencing the issue; the issue still read as fully open. Worth a habit nudge: when a multi-part issue gets partially executed by a different PR, close that scope on the issue with a back-link so it doesn't lurk. (c) The `add-paper` family is the documented example where the provenance-pin pattern's one-way-pull bites: synthesis happened in template, dotfiles stayed at baseline, no mechanism pulls the synthesis back. If add-paper were ever re-synced from dotfiles by SHA, the synthesis would be lost. **Reviewer pass + PR:** ran `nori-code-reviewer` per the docs-branch heuristic from 2026-06-05 — surfaced 2 MEDIUM + 1 LOW addressed in commit `9f9d006`. M1: runtime-detection block at lines 8-28 of each per-protocol skill enumerated only git verbs for sandbox REST translation, not arbitrary file writes — generalized line 24 to "translate every `git add` / `git commit` / `git push` and any other file write in this skill". M2: persist mechanic under-specified — added "appended after the last existing `- **...:**` line in 'Operating preferences'; if the line already exists (race with a concurrent edit), leave it as-is." L1: "first paper" → "treat this as a first-use case" (doesn't over-claim what's behind the absent field). 2 LOW dismissed (em-dash/Enter parenthetical — agents generalize; STATUS-length style note — this repo's density is intentional, matches existing 2026-06-04 / 2026-06-05 entries). PR #22 created OPEN/MERGEABLE/CLEAN, no CI configured (matches PR #19 / #20). https://github.com/danparshall/claude_researcher/pull/22 . **Skipped step 14 `/loop`** (auto-poll review threads every 5 min) — small docs PR Dan reviewing himself; auto-respond loop isn't the workflow here. **Skipped `pr-review-guide` skill spawn** — drafted PR body inline (summary / risk highlights / suggested review path / test plan / reviewer notes) given small scope. **Hook bite:** initial `gh pr create --body "$(cat <<'EOF'`... heredoc tripped `\n#`-in-quoted-arg matcher on the `\n## Summary` markdown headers; routed via Write-then-run with `--body-file /tmp/...md` — documented pattern, worked first try, worth a corpus entry for the chain-matcher curator. **Heuristic worth promoting:** docs-branch sub-flow (skip TLP/SWE steps, run `nori-code-reviewer`, inline PR body) held up across PR #19, #20, #22 — three consecutive applications; worth promoting from a STATUS process-finding to either `RESEARCHER.md` or `finishing-a-development-branch`'s own SKILL.md, not done in this PR (scope). Untracked `template/scripts/resolve_append_conflict.py` left alone per multi-terminal protocol. Convo: [`docs/convos/20260606_add_paper_first_use_prompt.md`](docs/convos/20260606_add_paper_first_use_prompt.md).

- **2026-06-05 (PR #19 crashed-session recovery + merge; PR #20 BOOTSTRAP egress-revisit reminder shipped to ready-to-merge)** — Two-act session. **Act 1 — PR #19 recovery.** The prior session ran `nori-code-reviewer` against `task-skills` and crashed mid-verification of a flagged "genuine bug"; this session forensically located the crashed transcript at `.claude/projects/-Users-dan-code-claude-researcher/6dd742d4-….jsonl:712-713`, recovered the full reviewer punch list, and applied three fixes to `task-skills`: (a) **Critical — `home_repo` parser/template schema mismatch**: `personal_info.md.template:35` seeds the field as `- **Home repo:** <HOME_REPO>` but `task-create:74` and `task-remind:59` told the agent to grep for snake_case `home_repo: <owner>/<repo>` — naive parsing always missed the seeded line and silently fell through to the default, defeating the override. Aligned the skills to read by header name (matches sibling fields `Git fluency`/`Mode`). (b) **Important — `RESEARCHER.md` §2b field enumeration omitted `Home repo`**: added. (c) **Important — `task-triage:84,157` cited "Plan 09 Decision 6" twice** in prose; plans don't ship with the skill bundle so WebFetch-based agents can't resolve the citation; replaced with the in-prose reason. Dotfiles parallel commit `60fd8e6` synced the same three fixes to `~/code/dotfiles/nori-researcher/skills/{task-create,task-remind,task-triage}/SKILL.md` because `~/.claude/skills/` symlinks point straight at dotfiles, NOT via `claude_researcher`. Multi-terminal discipline preserved: targeted `git add <file>` kept dotfiles' unrelated WIP (chain-hook-maintenance, `nori.json`, tests) out of the fix commit. PR #19 merged with real merge commit [`5bb6da6`](https://github.com/danparshall/claude_researcher/commit/5bb6da6) (preserving `38468b1`/`7a0b53e` repin SHA-pin reachability per the standing convention). Issue #13 narrowed (title + comment) to its remaining scope — the BOOTSTRAP §1b auto-seeding subtask. **Act 2 — PR #20 (`bootstrap-egress-reminder`) operationalized #13's narrowed scope.** Two edits to `template/BOOTSTRAP.md`: (a) §1b language tightened from vague *"I can set up a reminder"* to concrete *"I'll file a reminder issue ... `task-remind` skill will surface it automatically"*; (b) Step 10 grew a new `### Egress-revisit reminder (first-time bootstrap only)` sub-step before the closing message that asks the user (if cross-chat-restart wiped the choice) whether they picked allow-all, then POSTs `[<today+7>] Revisit claude.ai network egress setting` with `task` label to `<HOME_REPO>` via the Issues API. Initial commit `8b89056`. **Reviewer pass on PR #20 surfaced 4 substantive findings:** C1 (`json.load` crash on non-JSON response — only key-missing was protected, not parse failure); C2 (silent 404 if HOME_REPO doesn't exist — Step 4 Batch 3 doesn't validate); S1+S2 (user prompt offered only allow-all/domain-list but agent instruction silently defaulted "unsure → file", contradicting §1b's allow-all-only promise); S3 (no dedupe — re-bootstrap would file a second reminder). Commit `5cc47bd` addressed all four: try/except wraps both response parses, repo-existence probe at top of bash block with skip-with-note on non-200, prompt expanded to include "or you're not sure" and Step 10 tightened to confirmed-allow-all-only (unsure users get a hand-off to file via `task-create`), GET-open-reminders dedupe check before label/issue POSTs. **Validation:** extracted the embedded bash block via Python regex and ran `bash -n` — exit 0 (well-formed). **S4 deferred — issue [#21](https://github.com/danparshall/claude_researcher/issues/21) filed**: bootstrap creates the `task` label with `--color 0052CC` but `task-create/SKILL.md:141` creates it without a color flag; first-creator-wins means visible color varies; would touch task-create in both claude_researcher and dotfiles (different scope from PR #20). Nits N1–N5 skipped as defensible. **PR #20 state:** OPEN, MERGEABLE, mergeStateStatus CLEAN, no CI configured. **Process findings:** (a) Crashed-session jsonl recovery is feasible — full Agent subagent transcripts persist; `grep` + targeted line reads is enough. The jsonl mtime can mismatch the last-content-timestamp (touched on session close), so don't rely on mtime alone for "which session crashed when." (b) `nori-code-reviewer` produced real value on both of the last two PRs — the standing `finish-development-branch` checklist is mostly N/A for docs-only branches (no tests, lint, typecheck, CI) but the reviewer step is the load-bearing one. Recommend: future invocations on docs branches should explicitly skip `/simplify` and `/code-simplifier` (TLP-specific) but always run `nori-code-reviewer`. (c) Repo placement decision: egress reminder lands in HOME_REPO, not specifically `claude_research_config`, because `task-remind` queries current-repo + home_repo at session-start; landing the reminder anywhere else breaks surfacing for users who customized home_repo. Convo: [`docs/convos/20260605_pr19_recovery_and_pr20_egress_reminder.md`](docs/convos/20260605_pr19_recovery_and_pr20_egress_reminder.md).

- **2026-06-04 (Plan 09 Phases 3-7 shipped on `task-skills`; ready for PR)** — Execution session continuing from the earlier 2026-06-04 Phase 1+2 session. **Phase 3 — port skills to `template/skills/`** shipped 3 commits to `task-skills`: `e1d8cac` task-create, `4d60250` task-remind, `cadb7d7` task-triage. Each port = verbatim body copy from dotfiles `nori-researcher/skills/<name>/SKILL.md@8b619b5` (body byte-identity verified via diff against post-frontmatter sections) plus provenance frontmatter (`nori_researcher_source: nori-researcher/skills/<name>/SKILL.md@8b619b5`) plus the affirmative-probe `## Runtime detection` block (matching the shipped pattern used by `finish-convo`/`update-docs`/`add-paper`). **Banner-form deviation from Plan 09 Decision 10:** Plan 09 specified a one-line `> **Runtime note (claude.ai):** ...` banner, but the actually-shipped Wave 2/3 pattern is the affirmative-probe block. Decision: match shipped (Dan confirmed). Each banner's verb list calibrated to the skill's actual surface: task-create has the widest list (gh issue create/list/edit/close, gh search issues, gh label list/create, gh repo view, gh api user, git add/commit/push); task-remind drops gh issue create and gh search issues; task-triage drops everything mutating (gh search issues + gh api user only) and explicitly calls out the read-only posture. **Phase 4 — wire `task-remind` into session-start:** `cb12829` adds a new `### 2d.5 — Check task reminders` step to `template/RESEARCHER.md` between §2d (SKILL_INDEX read) and §2e (first-message handshake), preserving existing §2e references (4 in-tree); `a965560` (dotfiles, pushed to `origin/main`) adds the parallel bullet to `nori-researcher/AGENTS.md` between the Pre-flight reads block and Determine-the-branch. **Phase 5 — cross-cutting docs:** `e359942` adds a new "Task management skills" section to `template/skills/SKILL_INDEX.md` between Knowledge-management and Writing & document workflow, with the three triplet entries plus a top-of-file status-block update mentioning the new section + gh verbs in the Runtime-detection translation paragraph + the dotfiles-8b619b5 pinned-to note; `3d3e8f2` adds a fifth tier-independent trait to `template/RESEARCHER.md` §0 Persona (`Briefly explain back-end behavior the user might want to understand`) operationalizing Decision 13's LIGHT-explanation principle, explicitly distinguished from "don't make decisions silently" (decisions vs. mechanism). **Phase 6.1 — local sync (bigger than the plan anticipated):** the dotfiles renames left `~/.nori/profiles/researcher/skills/{capture-task,triage-tasks}/SKILL.md` as broken symlinks pointing at non-existent dotfile paths; `~/.claude/skills/` (what the Claude Code harness actually reads) had stale **file copies** (not symlinks) from a past `sks switch researcher`; and `dotfiles/install.sh`'s `RESEARCHER_SKILLS` array still listed the old names. Three fixes: (a) manually swept the broken-symlink dirs in `~/.nori/profiles/researcher/skills/` + created new symlinks for task-create/task-remind/task-triage following install.sh's pattern; (b) `rm -rf` of `~/.claude/skills/{capture-task,triage-tasks}` + new symlinks for the three task-* skills (so the local CLI harness sees the new names); (c) `304e7cd` (dotfiles, pushed to `origin/main`) updated install.sh's RESEARCHER_SKILLS array. **Mid-session manifest re-discovery:** the harness re-scanned `~/.claude/skills/` mid-session and the "Available Skills" system reminder updated to include `task-create` / `task-remind` / `task-triage` and drop `capture-task` / `triage-tasks` — passive confirmation that the files are wired under the new names. **Phase 6.2 — CLI smoke test DEFERRED** to next fresh session (Dan's call): the multi-session steps (verify task-remind auto-fires at session-start, snooze flow) need a fresh session anyway, and the behavioral surface is mostly LLM-instruction-following so the local-files-exist confirmation already in hand is decent evidence. **Phase 7.1 — verification:** wrote `/tmp/plan09_verify.py` (Python per Dan's preference over the Plan 09 bash form), 9 structural checks → 21 sub-assertions, all pass except a Check-8 false positive (caught `basic_config` in `docs/convos/` historical narrative; Plan 09 Task 0.1's actual decision tree scopes to `template/`, where the count is 0). Effectively 21/21. **Pending in this session:** Phase 7.2 (run `tools/repin.py` for the BOOTSTRAP `personal_info.md.template` pin bump since `165aaae` edited that template) + finish-convo + Phase 7.4 (PR via finishing-a-development-branch). **Process findings:** (a) Plan 09's banner-form prescription was stale — the shipped pattern had been upgraded post-Plan-09 writing; surfaced and Dan picked the shipped form, captured the deviation in commit messages + here. (b) Plan 09 Task 6.1's "symlink update" framing assumed install.sh managed `~/.claude/skills/`, but it doesn't — it only manages `~/.nori/profiles/researcher/skills/`. The `~/.claude/skills/` dir is populated by `sks switch researcher`. This is a real maintenance gap worth flagging for future plans (skill renames need 3 surfaces to update, not 2). (c) Numbering-choice for `§2d.5` (parallel to §1.5 / §2.0 conventions) preserved 4 in-tree §2e references without sweeping. (d) Targeted `git add <file>` discipline maintained throughout despite dotfiles having concurrent uncommitted work from the chain-hook-maintenance agent. Convo: [`docs/convos/20260604_plan09_phase3_through_7_ship.md`](docs/convos/20260604_plan09_phase3_through_7_ship.md).

- **2026-06-04 (Plan 09 Phases 0-2 shipped on `task-skills`)** — Execution session against [`docs/plans/09_task_skills.md`](docs/plans/09_task_skills.md), written by the prior 2026-06-04 design session. Phase 0 precondition verified (issue #14 closed via PR #17; no stale `basic_config` in `template/`). **Phase 1 — dotfiles source of truth** shipped 3 commits to `~/code/dotfiles` (pushed to `origin/main`): `baa7120` task-create (rename of `capture-task` + new Step 2.5 "Ask when?" + home_repo routing + cross-repo back-link prompt + Common Mistakes additions, 213/161), `fe0d52d` task-remind (new skill: session-start reminder check with `[YYYY-MM-DD]` regex filter + close/snooze/skip menu with conditional strip-prefix + LIGHT-explanation discipline + 7 Common Mistakes entries, 155 new), `8b619b5` task-triage (rename of `triage-tasks` + surface date-prefix on inventory rows per Decision 6 + `cd...` → `git -C` cleanup + Filter-out Common Mistake, 16/9). **Phase 2 — `home_repo` config wiring** shipped 2 commits to `claude_researcher/task-skills`: `165aaae` (add `<HOME_REPO>` field under `## Operating preferences` in `template/templates/personal_info.md.template` alongside git fluency + mode, with narrative explanation), `f5f6eea` (extend BOOTSTRAP §4 Batch 3 from 3 to 4 questions — Home repo Q3 between Mode and paper-source domains; concrete dentist-reminder example; LIGHT-explanation paragraph for confused users; default = `<USERNAME>/claude_research_config` with Enter-to-accept). Convo commit `082e1a2`. **Branch state:** `task-skills` pushed to origin at `f5f6eea`. **Mid-session multi-terminal coordination:** another agent active in `~/code/dotfiles` on chain-hook-maintenance work (uncommitted `nori.json` + probes); paused after staging the first `git mv`, undid, asked the user, resumed when confirmed done. Targeted `git add <file>` discipline throughout meant the other agent's uncommitted work was never accidentally swept. **Process findings:** (a) Plan 09's Coupling Concerns + Decisions blocks did their job — when I caught myself re-deriving Decision 11/12, the plan's clear wording was the catch-and-revert trigger. (b) Port-time judgment: Plan 09 step-numbering said "insert Step 2.5" but my first pass renumbered downstream steps, creating a duplicate Step 4; fixed by restoring plan-faithful structure (Steps 1, 2, 2.5, 3, 4, 5, 6). (c) The `personal_info.md.template` was sparser than the plan implied (no `<GH_USER>`/`<TOPIC>` placeholders) — port-time call to slot `home_repo` under `## Operating preferences` rather than re-derive the schema. (d) Used TaskCreate (7 tasks) — added value mostly as a "did I push after committing?" checkpoint; the plan itself is the substantive checklist. **Pre-merge dependency surfaced:** `tools/repin.py` must bump BOOTSTRAP.md line 383's `personal_info.md.template` SHA-pin (currently `8c732081`) since `165aaae` edited that template — captured in `f5f6eea`'s commit message for Phase 7. **Pending:** Phases 3-7 of Plan 09 — port the 3 skills to `template/skills/` with provenance frontmatter (pinned to dotfiles `8b619b5`) + REST-adaptation banner, wire task-remind into RESEARCHER §2 + dotfiles AGENTS.md, SKILL_INDEX additions + RESEARCHER §0 LIGHT-explanation, CLI smoke test, repin + STATUS update + finish-convo + PR. Convo: [`docs/convos/20260604_plan09_phase1_dotfiles_ship.md`](docs/convos/20260604_plan09_phase1_dotfiles_ship.md).

- **2026-06-04 (Plan 08 written + Phase 1 + Phase 2 shipped on `onboarding-ux-cleanup`)** — Execution session against the 2026-06-03 think-aloud convo. Wrote [`docs/plans/08_onboarding_ux.md`](docs/plans/08_onboarding_ux.md) (374 lines) lifting Decisions Made + Resolutions sections; Captured Tasks (#11/#12/#13) deferred. Dispatched Phase 1 to a `general-purpose` subagent with worktree isolation (`.claude/worktrees/agent-a63ed158a7e2a96b1/`); 7 commits shipped in ~11 minutes: README slim-down, HUMANS.md durability subsection, BOOTSTRAP §§0/1b/2b prose pass (timing claim + allow-all egress default + PAT affordance "happy to explain"), §4 interview restructure (drop paper-naming + git-fluency concept-check reword + per-batch closers), RESEARCHER.md §4 NDA/IP project-repo isolation paragraph, terminology sweep dropping orchestration/fork/my VM/the VM/shell command from user-facing prose (egress UI-label survives in step-by-step instructions), HUMANS.md drop paper-naming field reference. Subagent accepted plan's Q1/Q2/Q3 recommendations as given; raised Q5 on standalone `repo` surviving in user-spoken scripts (BOOTSTRAP §0 + §8 + §10) — Dan's call: accept Q5 (rewording forces 3-way collision with "claude.ai Project" + "research project"; cost > benefit), but add an explanatory glossary. Mid-session glossary commit (`b230fe8`) in BOOTSTRAP §2a defines git / GitHub / repo with affordance pattern matching the PAT section, folding "do you have a GitHub account?" into the closing line to avoid double-asking. **Screenshots:** initially captured 4 PNGs filed in `template/reference/screenshots/` and referenced inline in BOOTSTRAP §1b/§2b (commit `0a67101`) — Dan asked whether agent could render them in chat; honest answer was no (`web_fetch` returns markdown source not rendered images, agent sees `![alt](path)` as literal text, and the "share the link" instruction was incomplete because relative path isn't a sharable URL). Pivoted (`f78df07`) to README "Setup at a glance" section with 2 user-facing screenshots (egress toggle + final PAT permissions state); dropped 2 unused PNGs (`create_form`, `add_permissions`); trimmed CAPTURED.md sidecar accordingly. **HUMANS.md final edit** (`c35cd3e`): noted Dan as maintainer of the Nori "Researcher" skillset (includes many academic-specific skills) plus web-only framing — "`claude_researcher` is basically as close as we could get to that skillset in a web-only format" — landed via em-dash insertion + semicolon-bridged web-only sentence in the existing Researcher paragraph. **Multi-session coordination:** task-skills session active in another terminal (convo at [`docs/convos/20260604_task_skills_design.md`](docs/convos/20260604_task_skills_design.md), branch `task-skills`); zero file overlap with this branch's diff today. Future-merge risks identified: task-skills wants `basic_config` → `claude_research_config` rename ([#14](https://github.com/danparshall/claude_researcher/issues/14)) to land first per their sequencing; Dan chose to continue this branch as planned and let the rename PR sweep references when it ships. `personal_info.md.template` + `RESEARCHER.md` may eventually have minor conflicts (different sections, low risk). **Branch state:** 12 commits ahead of origin pushed (`f8573c3..c35cd3e`); plan + 7 subagent commits + glossary follow-up + 4-screenshots commit (later partially superseded) + screenshot pivot + HUMANS attribution. **Pending:** PR via `finishing-a-development-branch` (Dan said "actually finish-branch"); `tools/repin.py` re-run before merge (README pins `2dec9af`, BOOTSTRAP substantially changed); Phase 1 dogfood read-through optional but high-leverage; `basic_config` rename coordination at merge time. **Process notes:** cwd silently moved from agent worktree back to main worktree mid-session — recovery via explicit absolute paths + `git -C <path>` everywhere; finishing-a-development-branch skill is SWE-shaped, adapting by skipping /simplify / tests / linters / type-check / webapp demo / CI and keeping the PR-creation backbone. Convo: [`docs/convos/20260604_plan08_onboarding_ux_ship.md`](docs/convos/20260604_plan08_onboarding_ux_ship.md).

- **2026-06-04 (task-skills design + branch opened + 2 issues filed)** — Design session for the "task reminder" capability that emerged from [issue #13](https://github.com/danparshall/claude_researcher/issues/13). Reframed the originally-locked decision (flat `basic_config/reminders.md` from the 2026-06-03 onboarding think-aloud) into a unified GH-issues-with-date-prefix-title approach, on the grounds that the "I emailed Sarah, remind me next week" use case needs persistent identity + history + a snoozeable date on the same object — issues handle this natively, a flat file would force state-splitting across two backends. **Architecture locked:** 3 kebab-case skills (`task-create`, `task-remind`, `task-triage`) over a single backend (GH issues with `task` label, date encoded as `[YYYY-MM-DD]` title prefix for cheap session-start filtering via metadata-only API call — no body reads needed regardless of issue count). **Scope:** `task-create` defaults to current repo with "my personal list" override routing to home repo; `task-remind` queries current repo + personal home with two labeled sections, points to `task-triage` for cross-repo view; `task-triage` queries all repos with `task` label (renamed `triage-tasks`). **Pushback during design:** pushed back on Dan's initial "current repo only" for `task-remind` because it quietly breaks issue #13's motivating egress-revisit case (reminder set during bootstrap lives in personal-home repo; user works in research repos for the next week; current-repo-only never sees the reminder fire). Accepted — "current + home" landed with two labeled sections + task-triage as escape valve. **Surprise surfaced mid-session:** `capture-task` and `triage-tasks` are *not* in `claude_researcher/template/skills/` today — they only live in `~/code/dotfiles/nori-researcher/skills/`. The existing source-of-truth pattern for all 6 currently-shared Researcher skills is: authored in dotfiles, ported to claude_researcher with provenance frontmatter + REST-adaptation banner. Locked: follow the existing pattern for the new task-skills (not the inversion I initially proposed). **New branch `task-skills`** opened off main (commit `f337595`); convo doc captures the design; plan + execution deferred to next session. **Two issues filed via existing `capture-task`** (bootstrapping irony noted): [#14](https://github.com/danparshall/claude_researcher/issues/14) (`basic_config` → `claude_research_config` rename — recommended as its own branch *ahead* of task-skills so the new skills land with the right default already in place), [#15](https://github.com/danparshall/claude_researcher/issues/15) (consider adding an "agent notes about the user" file as an agent-authored complement to user-authored `personal_info.md`, supporting the "periodic LIGHT explanation of back-end behavior" principle Dan surfaced). **Recommended sequencing across branches:** rename first (mechanical unblocker), then task-skills (builds the infra), then onboarding-UX (picks up clean pointers to both). **Behavioral principle for `RESEARCHER.md` §0:** "periodic LIGHT explanation of back-end behavior" — when the agent does something the user might want to understand (cross-repo back-link decisions, snooze affordances), brief explanation rather than silent execution. **Process notes:** convo-name handshake (§2e) was partial — proposed at start via AskUserQuestion, rejected, used `20260604_task_skills_design` without explicit re-confirmation. STATUS.md cross-branch staleness surfaced during finish-convo: `task-skills` STATUS doesn't carry the 2026-06-03 onboarding-ux-cleanup session entries (those live on that branch until merge); future merge will need conflict resolution. Convo: [`docs/convos/20260604_task_skills_design.md`](docs/convos/20260604_task_skills_design.md). **Suggested next session against this branch:** write `docs/plans/NN_task_skills.md` against the convo decisions — natural candidate for a fresh claude.ai web chat as dogfooding the very flow being built (plan-writing is mostly single-file text work, well-suited to web; the design work used CLI well for `gh`/`git`/dotfiles inspection but plan-writing doesn't need those).

- **2026-06-03 (Think-aloud findings + `onboarding-ux-cleanup` branch opened + 3 task issues filed)** — Dan ran an in-person think-aloud session with a new user attempting the bootstrap flow end-to-end; the dump touched README, HUMANS.md, `BOOTSTRAP.md` §1 egress, §2b PAT, and (by implication) `RESEARCHER.md` for the project-repo isolation rule. PR [#10](https://github.com/danparshall/claude_researcher/pull/10) merged first to keep `repin-tooling` scope clean (real merge commit `7383d5d` per SHA-pin reachability); `onboarding-ux-cleanup` opened off updated main. Convo doc [`docs/convos/20260603_onboarding_ux_think_aloud.md`](docs/convos/20260603_onboarding_ux_think_aloud.md) captures the raw dump organized by surface, then a follow-up triage closed the six open questions: **screenshots → `template/reference/screenshots/`** (referenced inline in BOOTSTRAP); **egress-revisit reminder → durable file in `claude_research_config`** (not claude.ai memory, which is static text with no scheduled-recall primitive), tracked at [#13](https://github.com/danparshall/claude_researcher/issues/13); **paper-naming dropped from bootstrap interview**, moved to first-use prompt in `add-paper`, tracked at [#12](https://github.com/danparshall/claude_researcher/issues/12); **PAT affordance is bootstrap-only** ("you don't need to understand this, ask me to explain if you want"); **git-fluency elicitation reworded** to lead with concept ("the program used to track all changes is called 'git' — are you familiar with it?") not the loaded word "fluency"; **sequencing locked** as prose+RESEARCHER → screenshots → website (website tracked at [#11](https://github.com/danparshall/claude_researcher/issues/11) for the hoi-polloi audience). Across-the-board addition to interview batches: "it's okay if you don't want to answer right now, and remember you can always ask me for explanations." Other decisions locked: Claude.ai = Pro/Team / GitHub = free tier OK (README phrasing); most content moves from README to BOOTSTRAP (README becomes a thin human-only entry); project ≡ repo / no cross-repo NDA-IP bleed rule lands in `RESEARCHER.md`; allow-all is the egress default with the week-out revisit pairing it. **Terms to drop from user-facing copy** flagged in the convo: orchestration, fork, repo (in narrative), shell commands, "my VM", egress (in narrative — UI label survives in instructions); "token" reframed since the audience hears "Anthropic-charged tokens." **No file edits beyond the convo doc this session** — implementation deferred to plan-driven execution next session. Three task issues filed (#11 website, #12 paper-naming first-save, #13 session-start reminders); `task` label created on the repo (didn't previously exist). Two minor process notes: Nori commit-author hook's literal-`\n\n` bug is fixed (2026-05-12 patch holding) but footer renders adjacent to subject line with no blank-line separator — cosmetic, low priority; chain-matcher hook tripped twice on `\n#` heredoc bodies, Write-then-run pattern used to route around. Convo: [`docs/convos/20260603_onboarding_ux_think_aloud.md`](docs/convos/20260603_onboarding_ux_think_aloud.md). **Suggested next session against this branch:** write `docs/plans/08_onboarding_ux.md` from the convo decisions — natural candidate for a fresh claude.ai web chat as dogfooding the very flow being fixed.

- **2026-05-21 (Re-pin tooling — and the egress fix's stale entry pin)** — Immediately after PR #8 (egress UI-agnostic rewrite) merged, a gap surfaced: the fix was on `main` but **not reachable by collaborators**. The README's bootstrap-entry URL was still SHA-pinned to `ea2918a` (pre-egress-rewrite), so a collaborator pasting the quick-start prompt would `web_fetch` the *old* Step 1; and `BOOTSTRAP.md`'s in-flow pin for `domain_allowlist.txt` still pointed at `d137580` (pre-a2-note). The SHA-pin maintenance burden flagged in the 2026-05-20 convo had fired on the first change after the pins were introduced — the egress PR shipped without the required bump. **Dan chose to fix the recurring problem, not just the instance:** write a re-pin script, then use it. Added [`tools/repin.py`](tools/repin.py) (rewrites the managed pinned URLs to current HEAD; makes the two commits the self-reference requires, since a commit can't embed its own SHA; refuses to run on `main`; does not push) + [`tools/test_repin.py`](tools/test_repin.py) (covers the `repin_refs` rewrite logic — SHA/`main` refs rewritten, unmanaged paths + other-repo URLs untouched, every-occurrence, idempotence; stdlib-only, runnable standalone or under pytest). TDD note: implementation was written before the RED phase ran (a skill deviation); recovered by mutation testing — stubbing `repin_refs` made exactly the 3 rewrite tests fail, confirming non-vacuous; 6/6 green after restore. Tooling committed `87f84ea`; ran `repin.py` → README re-pinned to `2dec9af`, `BOOTSTRAP.md` template URLs to `87f84ea` (commits `2dec9af`, `132d2cd`). **Merge constraint:** `repin-tooling` must merge with a real merge commit (README pins `2dec9af` by SHA). **Open:** the re-pin is scripted but still manual to invoke — a pre-merge hook / CI staleness check would close the loop fully; not built. Convo: [`docs/convos/20260521_repin_tooling.md`](docs/convos/20260521_repin_tooling.md).

- **2026-05-21 (Egress Step 1 made UI-agnostic)** — Right after the SHA-pin fix verified working, Dan reported (via screenshot) that claude.ai's network-egress Settings UI had changed: the old mode selector (`Package Managers Only` / `All`) + free-form **Domain Allow List** was replaced — at least on his free-plan account — by a single **"Allow network egress"** toggle, with no visible custom domain allow-list. `BOOTSTRAP.md` Step 1 scripted exact clicks against the old UI and no longer matched. **Per Dan's redirect** ("allow all domains for now; if you're given a whitelist option later, include these four at minimum"; UI variant may depend on account tier), Step 1b was rewritten **UI-agnostic** — guide by intent (enable egress; pick the most permissive option the UI offers, whether toggle / mode choice / custom allow-list; 4 GitHub domains are the minimum if a list exists), with an agent note that the UI varies and a "surface if unrecognized" escape hatch. The 1a probe (`curl api.github.com/zen`) remains the empirical arbiter, so the doc no longer needs to know the UI. Terminology sweep "Domain Allow List" → "network egress" across `BOOTSTRAP.md` (§1 header, fetch-mechanisms note, 2 troubleshooting entries) and `RESEARCHER.md` (session-start troubleshooting). Shipped commit [`c484f95`](https://github.com/danparshall/claude_researcher/commit/c484f95) (2 files, +19 −29); Dan reviewed §1b wording — "LGTM". A `nori-web-search-researcher` dispatched to confirm the new egress model timed out with no result; not retried. **Free-tier reachability — RESOLVED:** Dan tested on a free-plan account — enabling the "Allow network egress" toggle *does* let the sandbox reach `api.github.com`; the rewritten Step 1 works end-to-end on free tier. **`domain_allowlist.txt` follow-up — addressed:** per Dan's request, commit [`047f0f7`](https://github.com/danparshall/claude_researcher/commit/047f0f7) added a note to the `domain_allowlist.txt` header and the Step 4 Batch 3 question that the allow-list is moot when egress is set to allow all domains; a fuller reframe of the file was not pursued. Branch complete; PR [#8](https://github.com/danparshall/claude_researcher/pull/8). Convo: [`docs/convos/20260521_egress_ui_doc_update.md`](docs/convos/20260521_egress_ui_doc_update.md).

- **2026-05-20 (Bootstrap SHA-pin — stale `web_fetch` was serving the thin slice)** — Dan reported a test bootstrap "still using the thin check" and asked to make the kit usable for collaborators. Investigation traced a real bug: a fresh claude.ai chat following the README quick-start prompt received the **55-line thin-slice smoke-test BOOTSTRAP** (`6511a52`, 2026-05-09) instead of the 579-line production bootstrap. The repo was not at fault — `git` on every branch, a live `curl` of the raw CDN (fresh cache headers), and a Claude Code `WebFetch` all returned production. A user screenshot was decisive: the claude.ai agent literally announced *"This is a thin-slice test."* **Root cause: claude.ai's `web_fetch` tool served an ~11-day-stale cached snapshot** of `…/main/template/BOOTSTRAP.md` — the thin slice occupied that path for a few hours on 2026-05-09 before `7e13380` superseded it; `web_fetch`'s retrieval layer cached that window and never refreshed. This is the 2026-05-11 stale-fetch Known Issue, but landing on the entry-point file and ~11 days stale. **Fix shipped on `pin-bootstrap-fetch-shas`** (2 commits, merged to `main` 2026-05-20): SHA-pin the four agent-fetched `/main/` raw URLs so an immutable commit-SHA URL is served correct content even from a stale cache — README→`BOOTSTRAP.md` pinned to `ea2918a`; `BOOTSTRAP.md`'s 3 in-flow template URLs (lines 378/382/499) pinned to `d137580`. Two commits by necessity (a commit can't embed its own SHA). **Merge strategy constraint:** merged as a real merge commit, not squash/rebase — the README pins `ea2918a` by SHA and that commit must stay permanently reachable from `main` history or the raw URL eventually 404s after GC. Line 552 (`RESEARCHER.md` fallback) deliberately left on `/main/` — it is a fallback whose primary (clone-first) is already fresh. **Open item: SHA-bump maintenance has no enforcement** — whenever `BOOTSTRAP.md` or a pinned template changes, the corresponding SHA must be bumped, or the bug silently returns; wants a release checklist or rewrite script, not yet captured as a task. Convo: [`docs/convos/20260520_bootstrap_sha_pin.md`](docs/convos/20260520_bootstrap_sha_pin.md).

- **2026-05-13 late evening (Plan 07 execution — add-paper dispatcher refactor shipped)** — Execution session against Plan 07 (`docs/plans/07_add_paper_dispatcher_refactor.md`), written earlier the same evening by the prior session. All four deliverables landed on-spec in a single ship commit [`5ca183b`](https://github.com/danparshall/claude_researcher/commit/5ca183b) (4 files / 424 insertions / 204 deletions). **D1 — `paper-processing-academic/SKILL.md` created** (181 lines; plan target 170-200): Protocol A subset of the unified add-paper lifted into a self-contained skill, renumbered Steps 1-6, Protocol A summary template + `@article`/`@unpublished` BibTeX only, schema back-pointer to dispatcher's Scope section, Common Mistakes scoped to Protocol-A failure modes. **D2 — `paper-processing-institutional/SKILL.md` created** (207 lines; plan target 190-220): Protocol B subset PLUS the institutional Step 2 enrichment (three rules verbatim per Plan 07 lines 226-232: preserve acronym list verbatim, preserve boxes and figure captions with key data, strip decorative front matter — these are the true scope addition vs Plan 06's "keep Step 2 as-is" decision); Step 3 "Outlet" column guidance picked up from Andrea's lines 222-224; Common Mistakes adds "Reading only the executive summary" and "Stripping the acronym list during extraction." **D3 — `add-paper/SKILL.md` refactored to dispatcher** (278 → 101 lines; plan target ~80-100, 1 over): frontmatter description revised to "Triage skill"; TodoWrite shrunk to 2 entries; Scope enumerates the three dispatch targets (academic / institutional / non-paper); Step 0 triage content preserved verbatim from Plan 06 with Andrea's Notes-specific-to-Protocol-B routing hints (lines 244-249) folded into the borderline-case list; new Dispatch block at end of Step 0; Common Mistakes scoped to dispatcher-level only ("Forgetting Step 0 triage" / "Trying to do the full workflow in `add-paper`" anti-pattern per Plan 07 line 75 / "Reading config keys from the wrong file"). **D4 — `SKILL_INDEX.md` updated**: `add-paper` description revised to describe the triage role; two new entries (`paper-processing-academic`, `paper-processing-institutional`) inserted after `add-paper` in the knowledge-management section. **Verification**: source SHA `e0a736d` verified byte-identical to AITaxBID HEAD `abd1c54` (same SHAs as Plan 06's execution earlier the same day — no drift between plans 06 and 07); 10-check verification script ran clean after one mid-flight fix on Check 5 (a "Protocol A paper" cross-reference in the institutional skill's naming-ambiguity edge case rewritten to "academic-style paper — go back through triage if so" — preserves the routing hint without leaking the Protocol A label into the institutional file, satisfying the partitioning constraint and reading more naturally for an agent invoking the institutional skill directly). **Post-ship discussion of "what's left for functional":** agent prioritized empirical validation (no real beta session yet) over more porting; named `document-processing` (Wave 5) as the one branch in the just-shipped dispatcher that hits a wall, and issue #5 (style-profile meta-skill) as the only other live gap. **Issue [#6](https://github.com/danparshall/claude_researcher/issues/6) filed** on Dan's request — `Wave 5: port document-processing skill (third dispatch branch from add-paper)` — medium priority, not blocking beta. Plan 07's split surfaced the gap (dispatcher's third branch points at a non-existent skill) from a Plan-02-internal concern to a runtime-visible one. Source pinned to Andrea's `~/code/AITaxBID/skills/document_processing.md@80803bf` (277 lines). **Process findings:** Plan 07's "decisions confirmed at design time" block had a "TodoWrite renumbering" pre-emption that protected against an obvious agent-side instinct (try to coordinate per-protocol TodoWrite blocks with the dispatcher's) that would have been wrong; the renumbering-from-1 was correct. Used TaskCreate this session (10 tasks) — contrast with Plan 06's execution session which deliberately skipped TaskCreate. Heuristic refinement: TaskCreate is value-added when the work has discrete sub-steps with real ordering (pre-flight → D1-D4 → verification → ship → push); the plan-doc-is-checklist case still wins when the plan is a linear sequence of small edits. Execution ran ~30 min; under the plan's 45-60 min estimate. Pre-existing untracked `template/README.md` from another session left alone per multi-terminal protocol. Convo: [`docs/convos/20260513_plan07_addpaper_dispatcher_ship.md`](docs/convos/20260513_plan07_addpaper_dispatcher_ship.md). Skill ports tally: **17 → 19 SKILL.md files** (`add-paper` rewritten; `paper-processing-academic` + `paper-processing-institutional` added).

- **2026-05-13 evening (Plan 06 execution — W3.1 add-paper synthesis + Tier C schema split shipped)** — Execution session against Plan 06 (`docs/plans/06_w31_addpaper_synthesis.md`), written earlier today by the prior session. All 9 deliverables + 2 knock-on edits shipped in single commit [`454018f`](https://github.com/danparshall/claude_researcher/commit/454018f) (5 files / 220 insertions / 47 deletions). **`add-paper/SKILL.md` rewrite** (151 → 281 lines): Step 0 triage (academic vs institutional via Andrea's three-question rule); filename conventions parameterized via personal_info.md (academic default `{FirstAuthor}_{LastAuthor}__{Year}--{Slug}.pdf` with kebab-case `{Slug}`, institutional default `{Institution}_{ShortTitle}_{Year}.pdf`); dual-protocol summary template (a/b/c/d); Summary Evolution Principle paragraph; new BibTeX step gated on `BIB_FILE`; `document-processing` pointer (Wave 5 aspirational); dual provenance frontmatter (`nori_researcher_source` + `aitaxbid_source`) — first skill carrying both, establishing precedent for future synthesis ports. Pipeline-structure choice held per plan: parameterize Nori's 5-step shape by Protocol A/B instead of duplicating across A-suffix / B-suffix steps. **Tier C Option C locked**: per-user `paper_naming.*` formats stay in `personal_info.md`; per-project keys (`PROJECT_QUESTION`, `CONDITIONAL_SECTION`, `BIB_FILE`, `PAPERS_INDEX`, `paper_summaries.structure`) live in `STATUS.md` under `## Project parameters` — seeded in three locations (BOOTSTRAP Step 7 STATUS.md seed substituting `<PROJECT_QUESTION>` from Step 5b; init-research-repo Step 3 minimal-seed branch + idempotent append branch; add-paper's "Where config keys live" Scope note). `paper_summaries.structure: single-file` is the default; `per-file` is forward-compat-only — config knob exists but code path deferred per Plan 05 W3.1 out-of-scope (the skill warns user inline and falls back). **personal_info.md.template** split into `<PAPER_NAMING_ACADEMIC>` + `<PAPER_NAMING_INSTITUTIONAL>` (breaking change for alphas — old `Paper naming format` field is no longer read; per Dan's call "they're alphas and they deserve breaking changes"). **BOOTSTRAP** Batch 3 rewritten to ask both formats with defaults + "press Enter to accept both" affordance; Step 5b extended to capture `<PROJECT_QUESTION>` after `<TOPIC>` (same-sentence-is-fine affordance from plan's suggested phrasing); Step 5c knowledge_base path given a placeholder; Step 7 STATUS.md seed gets `## Project parameters` block between `## What this repo is` and `## Current state`. **RESEARCHER.md §2c** gains one sentence about Project parameters (no extra fetch since STATUS.md is already part of session-start). **Verification**: source verified byte-identical to scoping SHA `e0a736d` at execution time (AITaxBID HEAD `abd1c54`, but `paper_processing.md` unchanged since 2026-05-02); plan's 7-check verification script ran clean; Knock-on 3 cross-reference grep confirmed schema partitioning landed correctly (`personal_info.md` references only pair with `paper_naming.*`; `STATUS.md` references only pair with the per-project keys). **Port-time judgments (all small):** kebab-case picked for `{Slug}` (Plan 06 line 462 recommendation accepted; supersedes BOOTSTRAP Batch 3's prior snake_case `attention_is_all_you_need`); `## Project parameters` placement in init-research-repo's minimal-seed slot between `## Current Focus` and `## Recent Sessions` (matches canonical order); append-branch logic for init-research-repo Step 3 uses idempotent append-if-missing. **Issue [#3](https://github.com/danparshall/claude_researcher/issues/3) closed** with reference to ship commit. **Issue [#4](https://github.com/danparshall/claude_researcher/issues/4) remains open** for post-real-user-data scaling-discipline work. **Process findings:** Plan 06's "decisions confirmed at design time (do NOT re-litigate)" block did its job — prevented at least three temptations to expand scope (enriching Step 2 with Andrea's preserve-acronyms / preserve-boxes rules; collapsing paper_naming to a single format with smart `{Author}` placeholder; trying to read paper_naming from STATUS.md). Execution ran ~40 min; under the plan's 45-60 min estimate, confirming the value of front-loading port-time concretizations into the plan. Did NOT use TaskCreate despite three harness reminders — Plan 06 IS the checklist for this kind of execution session, matching yesterday's flesh-out heuristic. Convo: [`docs/convos/20260513_plan06_w31_addpaper_synthesis_ship.md`](docs/convos/20260513_plan06_w31_addpaper_synthesis_ship.md). Skill ports tally: **17 → still 17 SKILL.md files** (rewrite, not new add). Pre-existing untracked `template/README.md` from another session left alone per multi-terminal protocol.

- **2026-05-13 (Plan 05 Path 1 execution — W4.1 + W4.3 Tier A ports shipped)** — Execution session following yesterday's flesh-out. Dan's opening: *"Let's execute Plan 05 Path 1 — port W4.1 (iterative-writing-workflow) then W4.3 (branch-document-review via Compare API)."* Both ports shipped in commit [`9ca89b8`](https://github.com/danparshall/claude_researcher/commit/9ca89b8) (379 insertions / 9 deletions / 4 files). Sources verified byte-identical to scoping SHA `e0a736d` despite AITaxBID HEAD having moved (the `4a46580` commit touching `writing_skill.md` is ~27 seconds pre-`e0a736d`, also pre-scoping). **W4.1 — `iterative-writing-workflow`:** four transformations per spec (provenance frontmatter, RESEARCHER.md/STATUS.md rephrase, drop dated footer, style-profiles affordance). Pure-thought skill, no `## Runtime detection` banner. ~170 lines. **W4.3 — `branch-document-review`:** five transformations. Step 4 `git diff` replaced with GitHub Compare API call (W4.2 Option A locked yesterday); 300-file response cap surfaced as inline note. Andrea/Marta/RA name references stripped clean (`grep -c -i 'andrea|marta|aflopezluzuriaga'` → 0); project slug examples generalized. Spanish bracketed example kept (illustration, not name reference). H1 title kept (meaningful framing). **Branch-naming format → `MMdd`** — Dan port-time override of Plan 05's defer-to-Andrea `mmm-d` recommendation on **lexicographic-sort grounds**: text-month formats put Dec before Jan alphabetically; `m-d` numeric formats sort wrong on single-digit days (`5-15` before `5-2`). 2-digit month + 2-digit day, no year. Year omitted because branch lifetime is short. Reasoning captured inline in the skill body. **W4.4 — SKILL_INDEX:** new "Writing & document workflow" section between knowledge-management and working-style; top-of-file status block extended explaining why AITaxBID-sourced skills don't need the runtime-detection banner (pure-thought OR REST recipes inline). Establishes the precedent. **Plan 05 status updates:** W4.1/W4.2/W4.3/W4.4/W4.5 marked Shipped; branch-naming open question marked resolved with reasoning trail. **Process findings:** (a) The harness's session-start `git status` snapshot was stale — reported the prior session's flesh-out work as uncommitted when it had actually shipped at `61ac97a`. Initial commit-strategy framing rested on stale info but converged correctly (one ship commit today; spirit of Dan's "two commits" preference was honored across the two sessions). Worth flagging for future agents: re-verify with `git status --short` after reading STATUS.md. (b) The "defer to Andrea on methodological details" principle's escape hatch (Plan 05's "Recommendation made / Confirm at port time" pattern) worked cleanly — the W4.3 branch-naming override flipped on a concrete technical argument and the reasoning got captured inline so future readers don't have to dig through plan archaeology. (c) Used TaskCreate this session (4 tasks) — appropriate for execution work with clear sub-steps. Distinct from the flesh-out session which skipped TaskCreate as redundant with Plan 05 itself. Heuristic: execution sessions benefit from task tracking; planning sessions where the plan doc IS the checklist don't. (d) No major sycophancy moments — the one borderline call (keeping the Spanish bracketed example without confirming) is surfaced in the convo for Dan to override if he wants. **One pre-existing untracked artifact left alone:** `template/README.md` from another session (same pattern as 2026-05-12). Convo: [`docs/convos/20260513_writing_and_branch_review_ports.md`](docs/convos/20260513_writing_and_branch_review_ports.md). Skill ports tally: **15 → 17 SKILL.md files live** (Wave 1: 9 SWE carryovers; Wave 2/3: 6 Researcher; Wave 4 Tier A: 2 AITaxBID — added today).

- **2026-05-13 (Plan 05 flesh-out — Wave 4 specs + Tier C decision)** — Session opened with Dan's starter sentence from yesterday's suggested-next-session: *"Let's flesh out Plan 05 — start with sandbox-tooling recheck against `template/reference/SANDBOX_TOOLING.md` then move to `writing-skill`."* Surface ended up being Claude Code (CLI) rather than the WebUI assumed in Plan 05's stub header — driven by the discovery that Andrea's AITaxBID source files (`writing_skill.md`, `BranchWorkflow_Skill.md`, `paper_processing.md`) live on Dan's local filesystem only, with no PAT-readable mirror; CLI gives source access, WebUI doesn't. This flipped the source-access open question Dan added in commit `f54ea05` from a blocker into a resolved item: Wave 4 ports execute from CLI. **W4.0 collapsed to confirm-only** — cross-check against SANDBOX_TOOLING.md showed only `pandoc` is a hard Wave 4 dependency and it was already verified 2026-05-11 at 3.1.3; LaTeX/pptx/pypandoc deferred per tooling-protocol on-discovery cadence. **W4.1 spec'd** — Andrea's source SHA `e0a736d` (2026-05-02), pure-thought skill so no REST-adaptation banner needed (unlike Wave 2/3), four minor transformations (provenance frontmatter, CLAUDE.md → RESEARCHER.md rephrase, drop footer, leave content intact), naming question flagged (Andrea's YAML name `iterative-writing-workflow` recommended over stub's `writing-skill`), style-profiles note added per Dan's request with "if you don't have a style-profiles doc, you can create one" framing. **W4.2 decided** — Compare API (Option A) recommended with full reasoning matrix that re-evaluated the three-way option (Compare API / read-both-and-difflib / git clone + git diff) now that sandbox `git` 2.43.0 is confirmed; surfaced the 300-file response cap as a future-extension cliff. **W4.3 spec'd** — 10 keep-intact items + 5 adapt items (Step 4 Compare API translation, branch-naming `mmm-d` deferred to Andrea, provenance, drop footer, strip Andrea/Marta/RA name references), no REST banner because Andrea's Steps 1 and 6 already ship REST recipes verbatim. **W3.1 spec'd — bigger than the stub suggested.** Read Andrea's `paper_processing.md` (320 lines) end-to-end; the fold-in is a strict superset replacement, not an additive edit — 9 deliverables including a new BibTeX step (gated on `BIB_FILE` config). Three architectural issues surfaced: filename convention conflict (Nori `AuthorLast_Year__short_description.pdf` vs BOOTSTRAP smoke-test `{FirstAuthor}_{LastAuthor}__{Year}--{Slug}.pdf` vs Andrea `Author_shortTitle_year.pdf`); file-structure mismatch (Andrea per-file vs Nori single-file PAPER_SUMMARIES.md); Claude-status/User-status framework deferred. **Tier C decided** — Option C (extend STATUS.md role) recommended with full reasoning. Sycophancy-checked the call: Option B is closer than the recommendation suggests; if params proliferate to ~15+ fields the call should flip — re-evaluation trigger documented. Built the user-level vs project-level split table that retroactively cleaned up W3.1's schema deliverable: `paper_naming.academic_format` + `paper_naming.institutional_format` stay in personal_info.md (user-level); PROJECT_QUESTION / CONDITIONAL_SECTION / BIB_FILE / PAPERS_INDEX / `paper_summaries.structure` go in a new STATUS.md "Project parameters" section (per-project). Five knock-on edits enumerated; they bundle with W3.1 execution. **Process findings:** Dan caught a soft option-text framing ("W3.1 might benefit from clear scope before the bigger Wave 4 ports") — pushed back with "Why do you think W3.1 is needed before W4?" Honest answer: no technical reason, procedural softening. Useful catch; sycophancy-check discipline from RESEARCHER.md §0 working as intended. **Did NOT use TaskCreate** despite three harness reminders — Plan 05 itself served as the de-facto checklist; harness tasks for a doc-editing session would have been redundant. Plan 05 went from ~175-line stub to ~340 lines; W4.4 (SKILL_INDEX entry) and W4.5 (ship commit) remain stubs by design (mechanical, flesh out at execution time). All edits uncommitted at update-docs checkpoint. **Continuation later in the same session:** Dan raised six adjacent items about `add-paper` (multi-repo destination, "Beyond this summary" affordance, two-stage file structure with migration trigger, ~10% summary length with 2000-word ceiling, grep-based targeted reading, heading convention). Filed as [issue #4](https://github.com/danparshall/claude_researcher/issues/4) (add-paper: scaling discipline), sequenced after W3.1; Plan 05's W3.1 entry now references #4 as a companion follow-on. Three pushback moments worth recording: I proposed "ask when ambiguous" for multi-repo, Dan responded "I don't understand the question" so I overcorrected to "default to session repo, no auto-ask" — Dan came back to clarify he concurred with "only ask when ambiguous" all along, so the overcorrection had dropped a useful affordance. Final rule restored it: default to session repo + honor explicit overrides + ask only when destination is genuinely ambiguous. Pure 10% summary length breaks down on long monographs, so I proposed a 2000-word ceiling with Beyond-this-summary catching the overflow. Dan agreed Stage 1 (PAPER_INDEX only) doesn't earn its complexity, simplified to two stages. Convo: [`docs/convos/20260513_plan05_flesh_out.md`](docs/convos/20260513_plan05_flesh_out.md).

- **2026-05-12 (audit follow-up reconciliation + Plan 05 stub + Nori hook bug fix)** — Reconciled the 2026-05-09 AITaxBID audit's findings against current main (`37f77c4`): Tier B patterns (show-before-committing, tracker-not-past-chats, 3+-repetition codification, Parking Lot, "Don't infer — ask," interview discipline) all landed on RESEARCHER.md/BOOTSTRAP.md; all Tier A skill ports still open; Tier C per-skill block format open as architectural decision. Wave 3's `add-paper` ship dropped W3.1's AITaxBID synthesis (Step 0 academic-vs-institutional triage, Protocol B summary structure) — surfaced by grep against `template/skills/add-paper/SKILL.md` returning no matches for "protocol," "academic vs," "institutional," "triage," "step 0"; filed as [`#3`](https://github.com/danparshall/claude_researcher/issues/3). **Wrote Plan 05** ([`docs/plans/05_aitaxbid_followups.md`](docs/plans/05_aitaxbid_followups.md), commit `04f6ec0`, 174 lines) — stub-only scaffolding for Wave 4 (W4.0–W4.5) + W3.1 retrofit + Tier C decision (4 options enumerated). Bakes in two principles: defer to Andrea on methodological details for skills that are hers or overlap strongly (she's been doing pure-GitHub web-UI work for years; the `[bracketed comments]` convention, light-vs-heavy reviewer rule, Protocol B summary structure, etc. are her field-tested calls — adapt only where architecture forces it), and use Nori kebab-case naming convention (claude_researcher will integrate more strongly with Nori). Tier C scoping resolved in part: parameterization confirmed as belonging in user research repos (rules out Option D skip; narrows to Option B `PROJECT_PARAMS.md` vs Option C extend STATUS.md role), final pick deferred to WebUI. **HUMANS.md edit shipped:** "Where instructions to Claude live" section (commit `a535723`) explaining the surfaces — local `~/.claude/CLAUDE.md` / per-repo CLAUDE.md, claude.ai Settings Personal preferences, Project Instructions, upstream `RESEARCHER.md`, `personal_info.md` in claude_research_config — with `personal_info.md` framed as the canonical home and in-conversation update examples. **README edits:** opening paragraph tightened with goal/accessibility sentence (commit `f3a00c3`), grammar fix follow-up (`37f77c4` — rebased from broken `24d2935`); `template/README.md` deprecated outright (in `f3a00c3`) since dual maintenance had drifted (still referenced `CLAUDE.md` after rename to `RESEARCHER.md`) and Phase 10 will need a README pass anyway. Issue [`#2`](https://github.com/danparshall/claude_researcher/issues/2) filed for env-indicator-in-convo-names (web/cli marker after date prefix) with three open questions (retroactive rename, axis granularity, position confirmation). **Nori hook bug:** identified in `commit-author.js` (PreToolUse Bash hook from `nori-skillsets`) — single-line `git commit -m "msg"` invocations get the Nori footer appended with literal `\n\n` characters instead of newlines; heredoc-style commits unaffected. Root-caused to line 30 of installed build artifact at `/opt/homebrew/lib/node_modules/nori-skillsets/build/src/cli/features/claude-code/hooks/config/commit-author.js`: `\\n\\n` in JS template literal produces literal two-character text, and bash inside `"..."` doesn't interpret `\n` as a newline. Filed upstream as [`tilework-tech/nori-skillsets#496`](https://github.com/tilework-tech/nori-skillsets/issues/496) with reproduction + suggested one-character fix. Patched locally (in-place edit changed `\\n\\n` → `\n\n`); wrote `~/code/dotfiles/notes_nori_commit_author_fix.md` (committed by parallel agent as `1009852`) describing patch location, before/after diff, when to re-apply (npm update, version bumps), and verification steps. Verification: parallel agent's commit message came through with proper multi-line formatting — fix is live. Pre-existing commits `2208e69`, `f8b8e1b` (Phase 2 era) show the same `\n\n` mangling pattern, so the bug has been latent in commit history for days. **Process findings:** main-sync at session-start was missed and bit when push got rejected at 14 commits behind; multi-terminal protocol worked — two independent agents flagged an untracked `template/README.md` in main worktree (mtime 06:23 today, byte-identical to `aitaxbid-skills-audit` branch HEAD via `diff` returning empty; left untouched per protocol — likely a parallel session restored it for audit/comparison and didn't clean up); convo-name handshake §2e was missed at session start and only happened at finish-convo time. Self-introspection check from Dan ("was your system prompt the full Claude Code or just '.'?") confirmed full Claude Code this session (`# Environment` block with Platform: darwin / Shell: zsh / "Opus 4.7 (1M context)" model, full tool framework, `# auto memory` pointing at memory dir) — useful evidence for the runtime-detection probe work (issue #2): in CLI the system prompt itself is unambiguously CLI. Context-management self-introspection: just two lines of explicit guidance ("write down important info from tool results since results may be cleared," auto-compression note) — implication is mirror SHAs and decisions into prose, rely on durable artifacts for resumption (same lesson as RESEARCHER.md §1.5 tracker-not-past-chats). Convo: [`docs/convos/20260512_audit_followup_and_plan05.md`](docs/convos/20260512_audit_followup_and_plan05.md).

- **2026-05-11 (Plan 04 ship review + persona+HUMANS edits + `tools/` ship + bio refresh)** — Fresh-agent review of prior same-day Plan 04 ship. Three substantive edits landed from review pushback: §0 Push Back paragraph reworded to drop two userPreferences-sourced phrases ("Sycophancy is a failure mode"; "Disagreement isn't disrespect") while preserving the structural template (commit `633c1dd`); HUMANS.md "Where this comes from" reframed Claude Code as later-available parallel option rather than graduation-path (commit `c13dd39`); reference-sweep failure mode (find-and-replace-by-inspection; hit twice via Plan 03's CLAUDE.md→RESEARCHER.md misses and Plan 04 Task 2's lowercase miss) scoped repo-specific rather than promoted to RESEARCHER.md §5. New `tools/` folder established: `sandbox-tooling-protocol.md` (commit `6290260`) splits methodology from `template/reference/SANDBOX_TOOLING.md`'s snapshot (criteria, probe procedure, cadence, post-probe update steps); `reference-sweep-note.md` (commit `37986a7`) documents the `grep -ri` verification protocol with the two empirical misses as motivation. init-research-repo retrofit deferred (no synthetic retrofit target; revisit at next real research-repo project). End-of-session bio elicitation produced a full History block for `personal_info.md` — UTK PhD on spin-phonon coupling in pnictides via neutron scattering, CU Boulder postdoc, NIST BT-7 Instrument Scientist, DS 2016–2025 (MITRE/aviation safety, edtech, IT), AI policy pivot 2026 with Canary Institute as the home, `claude-exit` + welfare-agnostic posture noted as cross-Claude calibration anchor. Git fluency `occasional` → `fluent`. Shipped to `claude_research_config/personal_info.md` (commit `0672b28`). PAT scope discovery: write to `claude_research_config` succeeded despite Project Instructions docstring suggesting read-only — worth a future audit or docstring correction. Convo: [`docs/convos/20260511_plan04_ship_review.md`](docs/convos/20260511_plan04_ship_review.md).

- **2026-05-11 (Plan 04 ship + audit follow-ups + §0 Persona + init-research-repo refactor)** — Later same-day session after the Plan 03 ship + Plan 04 draft. All four Plan 04 tasks shipped in ~30 minutes: sandbox tooling matrix (commit `2746090` — pypdf 5.9.0, pandoc 3.1.3, python-docx 1.2.0, git 2.43.0 all pre-installed on Ubuntu 24.04, collapsing the medium-confidence risk that Wave 4 would need a difflib redesign); "Custom Instructions" → "Project Instructions" sweep across 10 files / 31 occurrences (commits `83f0bc3`–`9e9f7ba`); `Surface: claude.ai | claude-code` field added to convo template in `update-docs/SKILL.md` (commit `585d94a`); §2e handshake extended with deterministic slug→title mapping rule + 3 illustrative examples + Claude Code skip note (commit `8531ed2`). **Audit during Task 3 surfaced Plan 03 reference-sweep misses:** 3 stale `CLAUDE.md` refs in `SKILL_INDEX.md` (commit `fea62ec`), 1 in `update-docs/SKILL.md` (commit `9eaaa15`), `ATTRIBUTION.md` line 24 rewritten (commit `ca55311` — also fixed lowercase "custom instructions" the case-sensitive Task 2 sweep missed, and removed the deprecated `_PROJECT_INSTRUCTIONS.md` upload pattern reference), `RESEARCHER.md` ↔ HUMANS.md cross-reference added in preamble (commit `fc6a002`). **Mid-session, Dan pushed HUMANS.md "Where instructions to Claude live"** (`a535723`) establishing four-surface vocabulary (Personal preferences, Project Instructions, RESEARCHER.md, personal_info.md) and deleted `template/README.md` outright as the (4b) audit recommendation (`f3a00c3`). **§0 Persona section added to `RESEARCHER.md`** (commit `0c99e5b`, ~33 lines) — four tier-independent traits (follow instructions, push back on bad ideas, don't make decisions silently, stay organized) with a research-collaborator framing paragraph; persona precedes calibration, outline updated. **init-research-repo refactored** (commit `2ae8294`) to remove all CLAUDE.md operations — the "Research Context" content was persona-level leaking into per-repo files. Preamble in "## The Process" makes the persona-vs-per-repo distinction explicit and notes Claude Code users can use `RESEARCHER.md` as their `~/.claude/CLAUDE.md`. Convo: [`docs/convos/20260511_tooling_and_terminology.md`](docs/convos/20260511_tooling_and_terminology.md). **Discipline note:** finish-convo run properly (skill read, announce-on-use practiced) rather than improvised — closing the gap noted in Plan 03's wrap.

- **2026-05-11 (Plan 03 clone-first ship + Phase 4.6 retrofit)** — Nine commits, all of Plan 03 plus all of Phase 4.6 shipped in one session. **Plan 03 motivation confirmed live in this session's own bootstrap:** the agent's session-start WebFetch of `template/CLAUDE.md` from `raw.githubusercontent.com` returned a pre-`31bb358` version missing yesterday's convo-name handshake, catch-up-source, and artifact-graph sections; user caught the omission ("did you get a note to propose a convo name up-front?") and the agent verified the discrepancy against the Contents API. The same pathology Plan 03 documents in its Known Issue entry, observed during Plan 03's own execution. **Plan 03 work:** Task 1 — wrote `template/RESEARCHER.md` with new §2.0 (clone command + freshness model + fallback), §2d/§5 reading from local clone, appendix entry rewritten from "~5 minutes" to "24+ hours" empirical (commit `915a094`); reference sweep across README (`53cc57b`), BOOTSTRAP (`4eb2f81`), 01_initial_build (`cf16fb8`, principled split — Phase 4 historical descriptions and tracker row left intact, forward-looking refs updated), 02_skill_ports (`2134492`); `template/CLAUDE.md` deleted outright (`28c36f8`) — user accepted the back-compat cost given <48-hour-old repo + alpha status, no stub keeper. Task 2 — `_PROJECT_INSTRUCTIONS.md.template` slimmed 82→37 lines (4375→1831 bytes), removed the "Why this uses the REST API" preamble (contained the empirically-wrong "git clone won't work" claim), removed the detailed REST recipes (live in RESEARCHER.md + skills); kept `<TOKEN>`/`<USERNAME>`/`<REPO>` placeholders for BOOTSTRAP substitution compatibility, kept yesterday's Ordering rule (updated to RESEARCHER.md). Commit `b599058`. Task 3 — STATUS Known Issue entry (`0003d5e`) documenting the 24+ hour raw-CDN staleness with dual evidence (SKILL_INDEX 2026-05-10, CLAUDE.md this session). **Phase 4.6 work (added mid-session, single commit `86dfeb3`):** §1.5 Resumption discipline added as standalone pre-fetch section (lifted from §2e's Catch-up source paragraph, which collapsed to a one-line backreference) — top-of-file placement so the rule applies before any session-start fetching, not after; §5 Working conventions sub-block with three rules — don't-infer-ask, show-before-committing, codify-after-third-repetition; existing Confirmation gates section reframed as the *emphatic* cases of show-before-commit, not the only cases; new §8 Parking Lot with two seed items (banner-vs-proper-REST empirical question, Phase 9 collaborator walkthrough awaiting candidate); document-structure outline at top updated to include §1.5 and §8. RESEARCHER.md now 462 lines. **Convo-name handshake:** `20260511_clone_first_ship` (proposed and confirmed mid-session after the agent realized the handshake instruction was missing from its session-start fetch). **Manual follow-ups required:** (1) paste slim `_PROJECT_INSTRUCTIONS.md.template` content into **Project Instructions** (correct UI terminology) of both this Project and `research-knowledge-base`, with real-value substitutions, before next session in either; (2) fresh-chat verification of clone-first session start. Convo: [`docs/convos/20260511_clone_first_ship.md`](docs/convos/20260511_clone_first_ship.md). **Continuation later in the same session — Plan 04 drafted + three procedural lessons surfaced.** After the Plan 03 + Phase 4.6 wrap, user asked about the next phase and (notably) whether `finish-convo` had actually been run; agent admitted it had improvised — written the wrap artifacts but bypassed the skill itself, including the announcement step that gives the user scrollback affordance. Re-read finish-convo and update-docs, confirmed substantive equivalence of shipped artifacts but procedural miss; the show-before-committing rule shipped in Phase 4.6 §5 would have caught this. Three additional findings: (a) **terminology correction** — Anthropic UI uses "Project Instructions" not "Custom Instructions"; multiple docs use the wrong term, sweep needed; (b) **2026-05-10 convo origin** — confirmed Claude Code via `~/.claude/skills/` path references in the convo body (claude.ai sandbox can't see that path); structural observation that convo files don't record which surface they came from, distinguishing matters for honest evidence on the banner-vs-proper-REST §8 Parking Lot question; (c) **human-readable chat title at §2e handshake** — user proposed offering both file slug (canonical) and derived chat title (presentation layer for WebUI scrollback) at the handshake, with deterministic slug→title mapping rule. All four items folded into **Plan 04** ([`docs/plans/04_sandbox_tooling_and_companion_cleanups.md`](docs/plans/04_sandbox_tooling_and_companion_cleanups.md), commit `2781b45`): Task 1 sandbox tooling matrix (the headline, unblocks Wave 4), Task 2 "Custom Instructions" → "Project Instructions" sweep, Task 3 `Surface` field added to convo template, Task 4 human-readable chat title at §2e handshake. STATUS Plans-list + Suggested-next-session updated to link Plan 04 (commit `910c80a`). Convo file appended with continuation section covering all of this (commit `3072fd3`). Side-finding worth tracking: update-docs's `<required>` block tells the agent to use `TodoWrite`, which doesn't exist in claude.ai — banner-vs-proper-REST translation only covers git idioms, not arbitrary tool name gaps. Open question for whoever picks up Plan 04: track in §8 Parking Lot or STATUS Open Questions. Total commits this session: **14** (9 Plan 03 + Phase 4.6 + wrap, 3 finish-convo wrap artifacts, 2 Plan 04 + STATUS link, plus this convo append + STATUS continuation = 14).

- **2026-05-11 (dogfooding pass — sysprompt comparison, stale-CDN finding, clone-first design, HUMANS.md ship)** — Dan opened a dogfooding session to switch his own primary use from Claude Code to claude.ai, so friction points surface for him before beta users hit them. Three substantive phases. (1) **Sysprompt comparison across surfaces.** Canonical claude.ai prompt at `platform.claude.com/docs/en/release-notes/system-prompts` (covers Sonnet 4.6, Opus 4.6, Opus 4.5, Haiku 4.5, Sonnet 4.5; Opus 4.7 also published but truncated from fetch). Best Claude Code reference is community-maintained `Piebald-AI/claude-code-system-prompts`, updated within minutes of each Code release; current v2.1.138 catalogs 110+ prompts (main agent + 24 tool descriptions + 3 sub-agent prompts + utility prompts + ~40 system reminders). Confirms the 2026-05-08 layer-analysis finding generalizes: claude.ai base is thin (CLAUDE.md does most work) while Code base is opinionated (CLAUDE.md does override work). (2) **Stale-raw-CDN finding.** Audit of SKILL_INDEX.md showed `raw.githubusercontent.com` serving 24+ hour stale stub content despite the file being correct on `main` via the Contents API. CLAUDE.md appendix anticipates ~5 minute staleness; reality is at least an order of magnitude longer. Affects any agent following §2d's WebFetch-from-raw pattern. (3) **Clone-first session-start design.** Empirical test: `git clone --depth 1` in the sandbox works in 335ms for the 896K template repo. Bypasses CDN staleness entirely, removes per-skill fetch latency, lets the agent `view`/`grep` over `template/skills/`. Lazy-loading frame preserved (agent still reads skill bodies on-demand, just from disk). **Shipped this session:** HUMANS.md as new long-form companion to README — explains Code/Nori heritage, gives a session walkthrough, surfaces the persona-as-archetype tip ("treat agents as collaborators, not interns — it produces measurably better work"), commit `09719f9`. README slimmed to tagline + pointer to HUMANS.md + quick-start, commit `5eb84f6`. **Deferred to next session via plan 03** ([`docs/plans/03_clone_first_and_companion_cleanups.md`](docs/plans/03_clone_first_and_companion_cleanups.md)): clone-first CLAUDE.md edit + `template/CLAUDE.md` → `template/RESEARCHER.md` rename + `_PROJECT_INSTRUCTIONS.md.template` slimming + STATUS Known Issues entry for stale-CDN. **Continuation later in the day — convo-handshake + catch-up rules shipped mid-session rather than deferred.** Dan flagged a workflow concern about whether agents auto-read chat history or project files before RESEARCHER.md/CLAUDE.md is fetched. Empirical investigation from inside the model: system prompt and Project Instructions both load passively before the agent's first inference; CLAUDE.md is fetched via tool call after the first inference; auto-memory is OFF for Dan so past chats are not auto-loaded, but the system prompt instructs the agent to call `conversation_search` on past-context cues and that call would fire in parallel with the CLAUDE.md fetch on turn one. `recent_chats` check returned one prior chat from this Project (May 9 handoff session) which did fetch CLAUDE.md first, but that session's message lacked past-context cues so it doesn't exercise the failure mode. **Resolution:** split the rule — ordering in `_PROJECT_INSTRUCTIONS.md.template` (one paragraph: "fetch CLAUDE.md before any other tool calls including past-chat search"), content in CLAUDE.md (handshake, catch-up source, artifact graph). Four commits shipped: `52abf78` (Project Instructions ordering rule), `31bb358` (CLAUDE.md §2e + §5 edits — catch-up source rule, convo-name handshake, artifact graph), `fc936db` (HUMANS.md tip expansions), `7c6a5c6` (update-docs skill convo-name dependency made explicit). **Manual step pending:** Dan to copy the updated `_PROJECT_INSTRUCTIONS.md.template` content into this Project's actual Custom Instructions so the ordering rule takes effect (existing Custom Instructions don't auto-sync from the template repo); the other three changes propagate automatically via next session's CLAUDE.md fetch. Convo: [`docs/convos/20260511_dogfooding_session.md`](docs/convos/20260511_dogfooding_session.md).
- **2026-05-10 (skill ports — initial ship: Waves 1+2+3 in one pass)** — Started intending Wave 0 (provenance + sync infrastructure) per [`02_skill_ports.md`](docs/plans/02_skill_ports.md), surfaced five up-front design decisions (script language, blob-vs-commit SHA, source-of-truth git repo for `~/.claude/skills/`, convo name, TDD stance for Wave 0 scripts), but user reframed before any of them got answered: *"the VERY first thing would be to just get skills in there at all, even if not perfect. Right now the onboarding collabs can't use this at all."* That collapsed Wave 0 into "deferred" and pushed Waves 1+2+3 forward into a fast verbatim ship. **Wave 1 (commit `9535e7e`):** 9 SWE carryovers (`brainstorming`, `test-driven-development`, `testing-anti-patterns`, `systematic-debugging`, `root-cause-tracing`, `creating-debug-tests-and-iterating`, `receiving-code-review`, `write-a-plan`, `handle-large-tasks`) copied verbatim from `~/.claude/skills/<name>/SKILL.md` into `template/skills/<name>/SKILL.md`. Pure-thought skills — they don't touch any of the 8 environmental axes — so they work in claude.ai's sandbox unmodified. **Waves 2+3 (commit `0bbd419`):** 6 Researcher skills (`finish-convo`, `update-docs`, `add-paper`, `audit-docs`, `audit-papers`, `init-research-repo`) shipped with one concession to honesty — a one-line banner inserted between frontmatter and body telling a claude.ai runtime agent to translate `git add`/`git commit`/`git push` into the REST `write_update`/`write_new` recipes from its Custom Instructions. The banner is "option B" from a three-way prompt (A=verbatim, B=banner, C=wait for proper REST adaptation); user picked B. Renamed `auditing-paper-summaries` (local skill dir) → `audit-papers` to match the URL convention SKILL_INDEX.md had already locked. SKILL_INDEX.md status block updated twice (after each commit) — now reads "all sections live" with the REST-adaptation caveat made explicit. All 15 URLs verified resolving with HTTP 200. **Empirical question this ship now opens:** does an agent reading the Researcher skill body + the CLAUDE.md REST recipes translate Claude-Code git idioms cleanly, or does it fumble (try to run `git push`, try to read `/Users/dan/.claude/skills/...` paths)? The first beta session is the test. If clean, banner is sufficient and proper Wave 2/3 REST adaptation can stay deferred. If fumbles, prioritize Wave 2/3 proper REST work. **What's NOT in this ship:** Wave 0 (provenance frontmatter + sync_carryovers + check_drift), Wave 4 (AITaxBID Tier A — `writing-skill`, `branch-document-review`), Phase 4.6 (CLAUDE.md retrofit from AITaxBID audit), Wave 5 (`document-processing`). Convo: [`docs/convos/20260510_skill_ports_initial_ship.md`](docs/convos/20260510_skill_ports_initial_ship.md).
- **2026-05-09 (evening — Phase 4 validation + AITaxBID PR merge + Phase 6 planning + publish strategy + 3-layer Nori chain + 02_skill_ports plan)** — Long continuation session. Six connected pieces of work. (1) **Phase 4 Step 9 validation passed end-to-end** in a fresh `research-knowledge-base` Project chat: WebFetch of upstream CLAUDE.md, env-var setup from Custom Instructions, PAT-authenticated fetches, SKILL_INDEX fetch, calibration to `light narration` from interaction style, recognition of `branches` workflow mode, name-and-context-aware greeting. **The runtime architecture is genuinely working.** Two small findings worth follow-on: (a) Step 8 agent tried sandbox curl to `raw.githubusercontent.com` (allow-list miss) and gracefully fell back to cached WebFetch — works but masks a real defect; (b) minor cosmetic — substitute-everywhere rule renders PAT in a tautological prose line. (2) **AITaxBID skills audit merged** via squash of PR #1 → `6397c33`. (3) **Phase 6 skill-port planning** translated audit recommendations into concrete plan tasks, commit `2f66ff6`. Phase 4.6 spec'd (5 Tier-B/C universal-rule retrofits to CLAUDE.md); Phase 6 expanded with 3 AITaxBID-sourced ports + Protocol-B fold-in to add-paper. (4) **Implementation Status Tracker added to 01's plan** to make nominal-vs-actual phase state legible — surfaced that Phase 7 (reference docs) is largely superseded by BOOTSTRAP.md's evolution; Phase 8 (self-walkthrough) effectively done by today's smoke test; Phase 10 mostly done implicitly. Real remaining v1 validation = Phase 9 collaborator walkthrough. (5) **Publish strategy resolved as status-quo + About section.** User clarified Andrea is a project collaborator — removes the only real concern with `docs/convos/` being public. Root README got an About section framing the project as a Dan + Andrea collaboration (commits `a18ef92`, `048d140`, `8a8b9cf` — last one fixing the .com → .net TLD). Andrea also invited as GitHub repo collaborator (`aflopezluzuriaga`, Write permission). (6) **3-layer Nori propagation chain established as architecture.** `claude_researcher` = Nori Researcher + non-CLI tricks. User authors Nori Researcher; Researcher depends on Nori SWE (which user does NOT author). Skills bucket by upstream relationship: SWE carryovers (cp + stamp), Researcher skills (dual-implementation maintenance), AITaxBID-derived (snapshot pattern). Provenance frontmatter convention: `nori_swe_source` / `nori_researcher_source` / `aitaxbid_source`, each with stamped SHA. (7) **New plan written:** [`docs/plans/02_skill_ports.md`](docs/plans/02_skill_ports.md) (commit `2d2ba27`) — wave-based execution plan optimized for time-to-first-usable-skill given beta users imminent. Wave 0 (provenance infra) → Wave 1 (9 SWE carryovers, first ship-ready state) → Wave 2 (session lifecycle) → Wave 3 (knowledge management) → Wave 4 (AITaxBID Tier A) → Wave 5 (deferred). Beta-user-imminent caveat: SKILL_INDEX.md should be trimmed to only-ported skills before any external pointer-sharing. Convo: [`docs/convos/20260509_phase6_skill_port_planning.md`](docs/convos/20260509_phase6_skill_port_planning.md).
- **2026-05-09 (Phase 4 — smoke test + 8 iteration commits)** — End-to-end smoke test of the bootstrap against incognito claude.ai chats (two runs) surfaced load-bearing empirical findings, all of which drove substantial restructuring of `BOOTSTRAP.md`: (1) egress changes do NOT propagate into already-open chats — fresh-chat restart is mandatory, not optional; (2) Domain Allow List UI doesn't accept bulk paste — domains entered one-at-a-time with per-domain "Add" buttons (no Save); (3) egress restriction is on Anthropic's server-side VM, not on the user's machine — pre-test framing about locked-down work-machine policies was wrong; (4) "Package Managers Only" preset enables on-the-fly `pip install` and is the right recommended default; (5) claude.ai user-level memory auto-loads into agent context — no separate "Read user prefs" step needed; (6) PAT lifecycle was undocumented. Restructure shipped: bootstrap collapsed from 13 steps to 11 with cleaner meta→user→project hierarchy; egress moves to Step 1; mode-check folded into interview's Batch 3 (now a `Mode` field in `personal_info.md`); old Step 3 (Read user prefs) deleted entirely; topic+repo moves AFTER interview ("user prefs first, then project"); paper-naming convention upgraded to `{FirstAuthor}_{LastAuthor}__{Year}--{Slug}.pdf` with a common-surname disambiguation rule (`SurnameF` for Smith/Jones/Patel/Singh + East Asian); Step 8 gains a PAT-lifecycle subsection (broad-PAT vs rotate-to-narrow vs per-project options); Step 2b token-handling rules clarified (ephemeral scratch explicitly permitted). The test agent's emergent behaviors validated several design patterns (pre-Step-0 meta-summary, prompt-injection scrutiny resolving correctly, VM mental-model internalization). Final commit of the day: `42648a4`. Smoke test paused at Step 2b's token-handling question; Steps 3–10 still untested. Convo addendum: [`docs/convos/20260509_phase4_runtime_and_skill_index.md`](docs/convos/20260509_phase4_runtime_and_skill_index.md) (Update section).
- **2026-05-09 (Phase 4 — CLAUDE.md + SKILL_INDEX stub + architecture pivot)** — Production `template/CLAUDE.md` written (~330 lines, calibration-first ordering with novice/fluent inline reminders, scripted confirmation gates, forward-compat hedge for v1.1 collaborator mode), plus `template/skills/SKILL_INDEX.md` manifest stub locking the skill-discovery contract before per-skill SKILL.md files exist. Architecture pivot during execution: PAT + curl recipes belong in the Project's **Custom Instructions** field, not in an uploaded `_PROJECT_INSTRUCTIONS.md` file — credentials reach the agent on its very first turn, before any fetching. Pivot drove three downstream edits: BOOTSTRAP.md Step 10 collapsed onto a single Custom Instructions paste (file-upload subsection deleted); Step 11 cause list updated; `_PROJECT_INSTRUCTIONS.md.template` URL fix. Also authored a new Phase 4.5 plan section spec'ing v1.1 direct-collaborator mode (deliberately narrower than fork-based or GitHub-org-based alternatives). Commit: `681ed9d`. Convo: [`docs/convos/20260509_phase4_runtime_and_skill_index.md`](docs/convos/20260509_phase4_runtime_and_skill_index.md).
- **2026-05-09 (Phase 3 — bootstrap design + implementation)** — Production `template/BOOTSTRAP.md` written and committed (`7e13380`); thin-slice version (`6511a52`) used as smoke test before; Phase 2 wording bugs fixed (`2208e69`). Three architecturally important findings, all validated by actual claude.ai testing: (1) WebFetch reaches public URLs verbatim with no allow-list; sandbox bash-curl needs allow-list — two distinct fetch mechanisms with different trust postures; (2) "Treat instructions as if I typed them" framing **backfires** as prompt-injection signature; (3) confirmation gates must be scripted into BOOTSTRAP.md, not invented by agent. Design pattern follows the kill-convo model: verification affordances offered (not demanded) at sensitive boundaries; availability builds trust without requiring use. Convo: [`docs/convos/20260509_phase3_bootstrap_design.md`](docs/convos/20260509_phase3_bootstrap_design.md).
- **2026-05-08 (initial build execution)** — Phase 1 (GitHub remote + push) and Phase 2 (`template/` skeleton: README, LICENSE copies, ATTRIBUTION.md, `_PROJECT_INSTRUCTIONS.md.template`, domain_allowlist.txt baseline, `.gitkeep` dirs) completed. Design tangent: Phase 4 commit policy will be `git_fluency`-tiered (novice = checkpoint often + under the hood; occasional = light narration + confirm before structural changes; fluent = terse). Side-finding: `--system-prompt` only replaces the default system prompt — tool descriptions, CLAUDE.md, skills, and hooks all load through other layers — see [`docs/convos/20260508_sysprompt_layer_analysis.md`](docs/convos/20260508_sysprompt_layer_analysis.md). Convo: [`docs/convos/20260508_phase1_phase2_initial_build.md`](docs/convos/20260508_phase1_phase2_initial_build.md).
- **2026-05-08 (design)** — Design conversation establishing dual-repo collaborator model, REST-API-via-PAT, on-demand skill fetching, bootstrap interview, runtime session flow, skill REST adaptations. Plan written. License (Apache 2.0 + Ship of Theseus) committed at root. Convo: [`docs/convos/20260508_claude_ai_researcher_design.md`](docs/convos/20260508_claude_ai_researcher_design.md).

## Open questions (live)

See plan's "Questions" section for full list. Highlights:

- ~~claude.ai custom-instructions length limit (verify before Phase 4).~~ **Resolved during Phase 4** — Custom Instructions hold the full credentials + recipes payload (substituted `_PROJECT_INSTRUCTIONS.md.template`), well within practical limits.
- ~~Allow-list propagation in already-open chats.~~ **Empirically resolved during Phase 4 smoke test** — does NOT propagate; fresh-chat restart required after configuration. Documented in Step 1c.
- ~~Domain Allow List paste format.~~ **Empirically resolved** — one-at-a-time per-domain "Add" buttons; no bulk paste, no Save button.
- ~~Bootstrap end-to-end smoke test.~~ **Resolved 2026-05-09 evening** — Steps 0-9 validated end-to-end across two incognito claude.ai chats (bootstrap chat + validation chat). Step 9 in particular confirmed the runtime CLAUDE.md → PAT auth → personal_info.md → calibration → workflow_mode → SKILL_INDEX → context-aware greeting chain works.
- ~~pypdf availability in claude.ai's sandbox — likely OK now that "Package Managers Only" preset is documented as the recommended egress mode (allows `pip install` on-demand). Still requires explicit verification before Phase 5 task 31.~~ **Resolved 2026-05-11 (Plan 04 Task 1)** — `pypdf` 5.9.0 pre-installed; see `template/reference/SANDBOX_TOOLING.md`.
- ~~**Sandbox tooling matrix beyond pypdf** (new, post-AITaxBID audit). Phase 6 task 40b (`branch-document-review`) needs `pandoc` for the regeneration step; task 40c (`document-processing`) may want `python-docx`. Worth a single consolidated check in a fresh claude.ai chat → `template/reference/SANDBOX_TOOLING.md`.~~ **Resolved 2026-05-11 (Plan 04 Task 1)** — `pandoc` 3.1.3 and `python-docx` 1.2.0 also pre-installed; see [`template/reference/SANDBOX_TOOLING.md`](template/reference/SANDBOX_TOOLING.md).
- Fine-grained PAT cross-org write capability (relevant for v2 issue auto-file).
- ~~**Publish-strategy revisit.**~~ **Resolved 2026-05-09 evening.** Status quo confirmed (dev repo stays public, including `docs/convos/` and `docs/plans/`). Andrea Lopez-Luzuriaga is a collaborator on this project, so the AITaxBID audit naming her is fine. Root README updated with an "About" section explicitly framing the project as a Dan + Andrea collaboration sharing learnings with the academic community — which makes the candor of the convos read as transparency, not as internal-asides-leaked.
- Phase 5 vs Phase 6 ordering (helpers-first lets skills call them; skills-first lets helpers be inferred from skill needs). Slight preference for Phase 5 first.
- **branch-document-review diff approach** (new, post-AITaxBID audit). GitHub Compare API vs. read-both-and-difflib. Decide at port time (Phase 6 task 40b); slight preference for Compare API.
- **Per-user-repo `CLAUDE_TEMPLATE.md`** (new, post-AITaxBID audit). Andrea's pattern of project-specific CLAUDE.md with `Generic skill / project-specific implementation` blocks. Tracked in plan; deferred until Phase 6 ports demonstrate need.
- **`raw.githubusercontent.com` allow-list miss at BOOTSTRAP Step 8** (new, from Phase 4 smoke test). Either Step 1's paste instructions don't actually add `raw.githubusercontent.com` to the user's allow-list, or Step 8 needs an explicit "use WebFetch, not curl" callout. Step 9 validation passed despite this — agent fell back to cached WebFetch. Small follow-on, not a blocker.

## Known issues

- **Nori `commit-author.js` hook produces malformed commit messages.** Inserts literal `\n\n` escape sequences instead of newlines, collapsing every commit into a single subject line with visible backslash-n. Cosmetic only; affects every Dan-authored commit using the hook (multiple repos). Worth filing upstream against `tilework-tech/nori-skillsets`. Detail in [`docs/convos/20260508_phase1_phase2_initial_build.md`](docs/convos/20260508_phase1_phase2_initial_build.md).
- **`raw.githubusercontent.com` serves stale content for 24+ hours after a write.** Empirically observed on 2026-05-11 for `template/skills/SKILL_INDEX.md` and again for `template/CLAUDE.md`: the Contents API returned the correct (updated) file on `main`, but `raw.githubusercontent.com` was still serving the old content from before the most recent commit — over 24 hours later. The pre-Plan-03 `CLAUDE.md` appendix anticipated ~5 minute staleness; reality is at least an order of magnitude longer in some cases. Plan 03's clone-first architecture (commit `28c36f8` and surrounding) routes around this on the primary path — sessions now `git clone` the upstream template at session start instead of WebFetching `raw.githubusercontent.com`. The fallback WebFetch path retains the exposure. If a future agent reports template content that doesn't match `main`, the raw CDN is the most likely culprit; the workaround is to fall back to the Contents API URL (`https://api.github.com/repos/danparshall/claude_researcher/contents/PATH`), to force a cache refresh by editing the file with a noop change, or simply to wait.
