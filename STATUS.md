# Status — claude_researcher (dev repo)

## What this repo is

Meta dev repo for building `claude_researcher`, a downstream fork of the Nori researcher Skillset adapted for claude.ai. **Not a research project itself.** Work is structured as one implementation plan followed in numbered phases.

## Critical note for fresh agents

The `docs/` layout is intentionally **flat** (`docs/convos/` + `docs/plans/`). Do NOT offer to scaffold to the standard `docs/active/<branch>/` layout — Dan explicitly chose flat for this single-purpose meta repo. Skip the "research structure check" prompt for this repo.

There are no `papers/`, `docs/active/`, or `docs/historical/` directories and there shouldn't be.

## Current state

- **Plans:**
  - [`docs/plans/01_initial_build.md`](docs/plans/01_initial_build.md) — original 10 phases + Phase 4.5 (v1.1 stub) + Phase 4.6 (CLAUDE.md retrofit). Has an **Implementation Status Tracker** at the top showing nominal-vs-actual phase state.
  - [`docs/plans/02_skill_ports.md`](docs/plans/02_skill_ports.md) — wave-based execution plan for Phase 5/6/4.6, ordered for time-to-first-usable-skill given beta users imminent. Five waves (0: provenance + sync infra; 1: SWE carryovers ship first; 2: session lifecycle; 3: knowledge management; 4: AITaxBID Tier A; 5: deferred). Establishes the 3-layer propagation chain (Nori SWE → Nori Researcher → claude_researcher) and the provenance-frontmatter convention.
- **Currently at:** **Phase 4 complete and end-to-end validated.** Smoke test in fresh incognito claude.ai chats covered Steps 0-9 cleanly: egress configuration, GitHub PAT setup, interview, repo creation (with 403-Administration-missing recovery path), file seeding (12 files via Contents API, 201/200 codes), claude.ai Project setup with Custom Instructions paste, and Step 9 validation in a fresh runtime chat — agent fetched CLAUDE.md, authenticated against `basic_config` and `research-knowledge-base`, calibrated tier dial from `personal_info.md`, recognized `branches` workflow mode, listed available skills, and offered natural next-step options. The runtime architecture (two fetch mechanisms, Custom Instructions credential payload, SKILL_INDEX manifest contract, branch-resolution-from-empty-repo fallback) is genuinely working.
- **Skill ports — initial ship complete (2026-05-10).** 15 SKILL.md files now live at `template/skills/<name>/SKILL.md` (commits `9535e7e`, `0bbd419`). Wave 1 (9 SWE carryovers) shipped verbatim from `~/.claude/skills/` since pure-thought skills don't touch the 8 environmental axes. Waves 2+3 (6 Researcher skills: `finish-convo`, `update-docs`, `add-paper`, `audit-docs`, `audit-papers`, `init-research-repo`) shipped with a one-line REST-adaptation banner inserted between frontmatter and body, telling the runtime agent to translate `git add`/`commit`/`push` into Custom-Instructions REST recipes. SKILL_INDEX.md status block updated to "all sections live" with the REST-adaptation caveat made explicit. Empty-kit problem (the prior beta-blocker) is gone. **Wave 0 (provenance + sync infra) deferred indefinitely** per ship-first reframe — re-evaluate once drift becomes a real concern. **Banner-vs-proper-REST is now an empirical question** that the first beta session will answer. Convo: [`docs/convos/20260510_skill_ports_initial_ship.md`](docs/convos/20260510_skill_ports_initial_ship.md).
- **AITaxBID skills audit merged** (PR #1 → `6397c33`). Recommendations translated into concrete plan tasks (`docs/convos/20260509_phase6_skill_port_planning.md`, commit `2f66ff6`): Phase 4.6 retrofit section added with 5 Tier-B/C CLAUDE.md universalization tasks; Phase 6 expanded with 3 new ports (writing-skill, branch-document-review, document-processing) plus a fold-in to existing add-paper for Step 0 academic-vs-institutional triage and Protocol B summary structure.
- **Phase 7+ largely re-scoped by BOOTSTRAP.md's evolution.** The Phase 4 smoke-test restructure absorbed most of Phase 7's planned reference docs (`WHY_REST.md`, `PAT_SETUP.md`, `PROJECT_SETUP.md`) into BOOTSTRAP.md inline. `template/reference/` is empty (`.gitkeep` only). Phase 8 (self-walkthrough) effectively done by today's smoke test. Phase 10 mostly done implicitly — repo public since 2026-05-09, root README has the canonical bootstrap prompt. **Real remaining work** in Phases 7-10: Phase 9 collaborator walkthrough (the only remaining test that materially de-risks v1), publish-strategy revisit (whether `docs/convos`/`docs/plans` should remain public long-term), and Phase 10 task 61 (placeholder issue to validate pre-filled URL).
- **Public entry point:** [`github.com/danparshall/claude_researcher`](https://github.com/danparshall/claude_researcher) — root README has the canonical bootstrap prompt as a copy-pasteable code block.
- **Suggested next session:** **Verification chat for clone-first** is the highest-priority immediate work — Plan 03 shipped (commits `915a094` through `0003d5e`) but needs a fresh-chat smoke test confirming the agent clones at session start and reads `RESEARCHER.md` from local disk cleanly. Manual step required first: copy the slimmed `_PROJECT_INSTRUCTIONS.md.template` content into both this Project's and `research-knowledge-base`'s Custom Instructions (substituting `<TOKEN>`/`<USERNAME>`/`<REPO>` with real values per Project). Phase 4.6 (RESEARCHER.md retrofit) is also done (commit `86dfeb3`), so the five universal rules (§1.5 Resumption discipline, §5 Working conventions: don't-infer-ask + show-before-committing + codify-after-three, §8 Parking Lot) are now live and exercise in the verification chat. After verification: **sandbox tooling matrix** (single fresh-chat check for `pypdf`/`pandoc`/`python-docx` availability → `template/reference/SANDBOX_TOOLING.md`), then **Wave 4 AITaxBID Tier A ports** (`writing-skill` first, ~1 hour; `branch-document-review` after sandbox-tooling resolves). The empirical test of the Researcher-skill banner approach (Wave 2/3) still awaits the first real beta session — now also surfaced in RESEARCHER.md's §8 Parking Lot. **Wave 0 (provenance + sync infrastructure) stays deferred** until drift becomes a real concern. **Phase 9 (collaborator walkthrough)** is the same empirical test from the user-experience side and now appears in §8 Parking Lot as an open item awaiting a candidate.
- **Branch:** main only. No research lines / feature branches. Audit work was done in `.worktrees/aitaxbid-skills-audit/` (worktree retained; branch merged + still on origin).
- **GitHub remote:** [`github.com/danparshall/claude_researcher`](https://github.com/danparshall/claude_researcher) — **public** (flipped 2026-05-09 to enable `raw.githubusercontent.com` reads from external claude.ai chats during bootstrap testing). Publish strategy resolved 2026-05-09 evening — status quo with `docs/` public; root README has an "About" section framing the project as a Dan Parshall + Andrea Lopez-Luzuriaga collaboration sharing learnings with the academic community.

### Phase 4.5 deferred (v1.1)

A "Phase 4.5 — Collaborator mode" section was added to the plan on 2026-05-09. Spec'd but not scheduled. Direct-collaborator on a private repo (professor + grad student model). Six sub-tasks: OWNER/USERNAME split in `_PROJECT_INSTRUCTIONS.md.template`, joining-vs-creating bootstrap branch, branch protection on `main` in `seed_repo.py`, `Role` field in `personal_info.md`, wrap-up flow split, PAT scope guidance for collaborators. v1 ships solo-only; v1.1 lands when the first real collaborator needs it.

### Phase 4.6 spec'd (2026-05-09 evening)

A "Phase 4.6 — CLAUDE.md retrofit from AITaxBID audit" section was added to the plan tonight. Five Tier-B/C patterns from Andrea Lopez-Luzuriaga's `AITaxBID/skills/` kit get promoted from per-step practice to universal rules in `template/CLAUDE.md`: tracker-not-past-chats discipline, 3+-repetition codification rule, Parking Lot section, "do not infer — ask," show-before-committing universalized. Source: [`docs/convos/20260509_aitaxbid_skills_audit.md`](docs/convos/20260509_aitaxbid_skills_audit.md). Estimated ~30-60 min; can interleave with Phase 5.

## Recent sessions

- **2026-05-11 (Plan 03 clone-first ship + Phase 4.6 retrofit)** — Nine commits, all of Plan 03 plus all of Phase 4.6 shipped in one session. **Plan 03 motivation confirmed live in this session's own bootstrap:** the agent's session-start WebFetch of `template/CLAUDE.md` from `raw.githubusercontent.com` returned a pre-`31bb358` version missing yesterday's convo-name handshake, catch-up-source, and artifact-graph sections; user caught the omission ("did you get a note to propose a convo name up-front?") and the agent verified the discrepancy against the Contents API. The same pathology Plan 03 documents in its Known Issue entry, observed during Plan 03's own execution. **Plan 03 work:** Task 1 — wrote `template/RESEARCHER.md` with new §2.0 (clone command + freshness model + fallback), §2d/§5 reading from local clone, appendix entry rewritten from "~5 minutes" to "24+ hours" empirical (commit `915a094`); reference sweep across README (`53cc57b`), BOOTSTRAP (`4eb2f81`), 01_initial_build (`cf16fb8`, principled split — Phase 4 historical descriptions and tracker row left intact, forward-looking refs updated), 02_skill_ports (`2134492`); `template/CLAUDE.md` deleted outright (`28c36f8`) — user accepted the back-compat cost given <48-hour-old repo + alpha status, no stub keeper. Task 2 — `_PROJECT_INSTRUCTIONS.md.template` slimmed 82→37 lines (4375→1831 bytes), removed the "Why this uses the REST API" preamble (contained the empirically-wrong "git clone won't work" claim), removed the detailed REST recipes (live in RESEARCHER.md + skills); kept `<TOKEN>`/`<USERNAME>`/`<REPO>` placeholders for BOOTSTRAP substitution compatibility, kept yesterday's Ordering rule (updated to RESEARCHER.md). Commit `b599058`. Task 3 — STATUS Known Issue entry (`0003d5e`) documenting the 24+ hour raw-CDN staleness with dual evidence (SKILL_INDEX 2026-05-10, CLAUDE.md this session). **Phase 4.6 work (added mid-session, single commit `86dfeb3`):** §1.5 Resumption discipline added as standalone pre-fetch section (lifted from §2e's Catch-up source paragraph, which collapsed to a one-line backreference) — top-of-file placement so the rule applies before any session-start fetching, not after; §5 Working conventions sub-block with three rules — don't-infer-ask, show-before-committing, codify-after-third-repetition; existing Confirmation gates section reframed as the *emphatic* cases of show-before-commit, not the only cases; new §8 Parking Lot with two seed items (banner-vs-proper-REST empirical question, Phase 9 collaborator walkthrough awaiting candidate); document-structure outline at top updated to include §1.5 and §8. RESEARCHER.md now 462 lines. **Convo-name handshake:** `20260511_clone_first_ship` (proposed and confirmed mid-session after the agent realized the handshake instruction was missing from its session-start fetch). **Manual follow-ups required:** (1) paste slim `_PROJECT_INSTRUCTIONS.md.template` content into Custom Instructions of both this Project and `research-knowledge-base`, with real-value substitutions, before next session in either; (2) fresh-chat verification of clone-first session start. Convo: [`docs/convos/20260511_clone_first_ship.md`](docs/convos/20260511_clone_first_ship.md).

- **2026-05-11 (dogfooding pass — sysprompt comparison, stale-CDN finding, clone-first design, HUMANS.md ship)** — Dan opened a dogfooding session to switch his own primary use from Claude Code to claude.ai, so friction points surface for him before beta users hit them. Three substantive phases. (1) **Sysprompt comparison across surfaces.** Canonical claude.ai prompt at `platform.claude.com/docs/en/release-notes/system-prompts` (covers Sonnet 4.6, Opus 4.6, Opus 4.5, Haiku 4.5, Sonnet 4.5; Opus 4.7 also published but truncated from fetch). Best Claude Code reference is community-maintained `Piebald-AI/claude-code-system-prompts`, updated within minutes of each Code release; current v2.1.138 catalogs 110+ prompts (main agent + 24 tool descriptions + 3 sub-agent prompts + utility prompts + ~40 system reminders). Confirms the 2026-05-08 layer-analysis finding generalizes: claude.ai base is thin (CLAUDE.md does most work) while Code base is opinionated (CLAUDE.md does override work). (2) **Stale-raw-CDN finding.** Audit of SKILL_INDEX.md showed `raw.githubusercontent.com` serving 24+ hour stale stub content despite the file being correct on `main` via the Contents API. CLAUDE.md appendix anticipates ~5 minute staleness; reality is at least an order of magnitude longer. Affects any agent following §2d's WebFetch-from-raw pattern. (3) **Clone-first session-start design.** Empirical test: `git clone --depth 1` in the sandbox works in 335ms for the 896K template repo. Bypasses CDN staleness entirely, removes per-skill fetch latency, lets the agent `view`/`grep` over `template/skills/`. Lazy-loading frame preserved (agent still reads skill bodies on-demand, just from disk). **Shipped this session:** HUMANS.md as new long-form companion to README — explains Code/Nori heritage, gives a session walkthrough, surfaces the persona-as-archetype tip ("treat agents as collaborators, not interns — it produces measurably better work"), commit `09719f9`. README slimmed to tagline + pointer to HUMANS.md + quick-start, commit `5eb84f6`. **Deferred to next session via plan 03** ([`docs/plans/03_clone_first_and_companion_cleanups.md`](docs/plans/03_clone_first_and_companion_cleanups.md)): clone-first CLAUDE.md edit + `template/CLAUDE.md` → `template/RESEARCHER.md` rename + `_PROJECT_INSTRUCTIONS.md.template` slimming + STATUS Known Issues entry for stale-CDN. **Continuation later in the day — convo-handshake + catch-up rules shipped mid-session rather than deferred.** Dan flagged a workflow concern about whether agents auto-read chat history or project files before RESEARCHER.md/CLAUDE.md is fetched. Empirical investigation from inside the model: system prompt and Project Instructions both load passively before the agent's first inference; CLAUDE.md is fetched via tool call after the first inference; auto-memory is OFF for Dan so past chats are not auto-loaded, but the system prompt instructs the agent to call `conversation_search` on past-context cues and that call would fire in parallel with the CLAUDE.md fetch on turn one. `recent_chats` check returned one prior chat from this Project (May 9 handoff session) which did fetch CLAUDE.md first, but that session's message lacked past-context cues so it doesn't exercise the failure mode. **Resolution:** split the rule — ordering in `_PROJECT_INSTRUCTIONS.md.template` (one paragraph: "fetch CLAUDE.md before any other tool calls including past-chat search"), content in CLAUDE.md (handshake, catch-up source, artifact graph). Four commits shipped: `52abf78` (Project Instructions ordering rule), `31bb358` (CLAUDE.md §2e + §5 edits — catch-up source rule, convo-name handshake, artifact graph), `fc936db` (HUMANS.md tip expansions), `7c6a5c6` (update-docs skill convo-name dependency made explicit). **Manual step pending:** Dan to copy the updated `_PROJECT_INSTRUCTIONS.md.template` content into this Project's actual Custom Instructions so the ordering rule takes effect (existing Custom Instructions don't auto-sync from the template repo); the other three changes propagate automatically via next session's CLAUDE.md fetch. Convo: [`docs/convos/20260511_dogfooding_session.md`](docs/convos/20260511_dogfooding_session.md).
- **2026-05-10 (skill ports — initial ship: Waves 1+2+3 in one pass)** — Started intending Wave 0 (provenance + sync infrastructure) per [`02_skill_ports.md`](docs/plans/02_skill_ports.md), surfaced five up-front design decisions (script language, blob-vs-commit SHA, source-of-truth git repo for `~/.claude/skills/`, convo name, TDD stance for Wave 0 scripts), but user reframed before any of them got answered: *"the VERY first thing would be to just get skills in there at all, even if not perfect. Right now the onboarding collabs can't use this at all."* That collapsed Wave 0 into "deferred" and pushed Waves 1+2+3 forward into a fast verbatim ship. **Wave 1 (commit `9535e7e`):** 9 SWE carryovers (`brainstorming`, `test-driven-development`, `testing-anti-patterns`, `systematic-debugging`, `root-cause-tracing`, `creating-debug-tests-and-iterating`, `receiving-code-review`, `write-a-plan`, `handle-large-tasks`) copied verbatim from `~/.claude/skills/<name>/SKILL.md` into `template/skills/<name>/SKILL.md`. Pure-thought skills — they don't touch any of the 8 environmental axes — so they work in claude.ai's sandbox unmodified. **Waves 2+3 (commit `0bbd419`):** 6 Researcher skills (`finish-convo`, `update-docs`, `add-paper`, `audit-docs`, `audit-papers`, `init-research-repo`) shipped with one concession to honesty — a one-line banner inserted between frontmatter and body telling a claude.ai runtime agent to translate `git add`/`git commit`/`git push` into the REST `write_update`/`write_new` recipes from its Custom Instructions. The banner is "option B" from a three-way prompt (A=verbatim, B=banner, C=wait for proper REST adaptation); user picked B. Renamed `auditing-paper-summaries` (local skill dir) → `audit-papers` to match the URL convention SKILL_INDEX.md had already locked. SKILL_INDEX.md status block updated twice (after each commit) — now reads "all sections live" with the REST-adaptation caveat made explicit. All 15 URLs verified resolving with HTTP 200. **Empirical question this ship now opens:** does an agent reading the Researcher skill body + the CLAUDE.md REST recipes translate Claude-Code git idioms cleanly, or does it fumble (try to run `git push`, try to read `/Users/dan/.claude/skills/...` paths)? The first beta session is the test. If clean, banner is sufficient and proper Wave 2/3 REST adaptation can stay deferred. If fumbles, prioritize Wave 2/3 proper REST work. **What's NOT in this ship:** Wave 0 (provenance frontmatter + sync_carryovers + check_drift), Wave 4 (AITaxBID Tier A — `writing-skill`, `branch-document-review`), Phase 4.6 (CLAUDE.md retrofit from AITaxBID audit), Wave 5 (`document-processing`). Convo: [`docs/convos/20260510_skill_ports_initial_ship.md`](docs/convos/20260510_skill_ports_initial_ship.md).
- **2026-05-09 (evening — Phase 4 validation + AITaxBID PR merge + Phase 6 planning + publish strategy + 3-layer Nori chain + 02_skill_ports plan)** — Long continuation session. Six connected pieces of work. (1) **Phase 4 Step 9 validation passed end-to-end** in a fresh `research-knowledge-base` Project chat: WebFetch of upstream CLAUDE.md, env-var setup from Custom Instructions, PAT-authenticated fetches, SKILL_INDEX fetch, calibration to `light narration` from interaction style, recognition of `branches` workflow mode, name-and-context-aware greeting. **The runtime architecture is genuinely working.** Two small findings worth follow-on: (a) Step 8 agent tried sandbox curl to `raw.githubusercontent.com` (allow-list miss) and gracefully fell back to cached WebFetch — works but masks a real defect; (b) minor cosmetic — substitute-everywhere rule renders PAT in a tautological prose line. (2) **AITaxBID skills audit merged** via squash of PR #1 → `6397c33`. (3) **Phase 6 skill-port planning** translated audit recommendations into concrete plan tasks, commit `2f66ff6`. Phase 4.6 spec'd (5 Tier-B/C universal-rule retrofits to CLAUDE.md); Phase 6 expanded with 3 AITaxBID-sourced ports + Protocol-B fold-in to add-paper. (4) **Implementation Status Tracker added to 01's plan** to make nominal-vs-actual phase state legible — surfaced that Phase 7 (reference docs) is largely superseded by BOOTSTRAP.md's evolution; Phase 8 (self-walkthrough) effectively done by today's smoke test; Phase 10 mostly done implicitly. Real remaining v1 validation = Phase 9 collaborator walkthrough. (5) **Publish strategy resolved as status-quo + About section.** User clarified Andrea is a project collaborator — removes the only real concern with `docs/convos/` being public. Root README got an About section framing the project as a Dan + Andrea collaboration (commits `a18ef92`, `048d140`, `8a8b9cf` — last one fixing the .com → .net TLD). Andrea also invited as GitHub repo collaborator (`aflopezluzuriaga`, Write permission). (6) **3-layer Nori propagation chain established as architecture.** `claude_researcher` = Nori Researcher + non-CLI tricks. User authors Nori Researcher; Researcher depends on Nori SWE (which user does NOT author). Skills bucket by upstream relationship: SWE carryovers (cp + stamp), Researcher skills (dual-implementation maintenance), AITaxBID-derived (snapshot pattern). Provenance frontmatter convention: `nori_swe_source` / `nori_researcher_source` / `aitaxbid_source`, each with stamped SHA. (7) **New plan written:** [`docs/plans/02_skill_ports.md`](docs/plans/02_skill_ports.md) (commit `2d2ba27`) — wave-based execution plan optimized for time-to-first-usable-skill given beta users imminent. Wave 0 (provenance infra) → Wave 1 (9 SWE carryovers, first ship-ready state) → Wave 2 (session lifecycle) → Wave 3 (knowledge management) → Wave 4 (AITaxBID Tier A) → Wave 5 (deferred). Beta-user-imminent caveat: SKILL_INDEX.md should be trimmed to only-ported skills before any external pointer-sharing. Convo: [`docs/convos/20260509_phase6_skill_port_planning.md`](docs/convos/20260509_phase6_skill_port_planning.md).
- **2026-05-09 (Phase 4 — smoke test + 8 iteration commits)** — End-to-end smoke test of the bootstrap against incognito claude.ai chats (two runs) surfaced load-bearing empirical findings, all of which drove substantial restructuring of `BOOTSTRAP.md`: (1) egress changes do NOT propagate into already-open chats — fresh-chat restart is mandatory, not optional; (2) Domain Allow List UI doesn't accept bulk paste — domains entered one-at-a-time with per-domain "Add" buttons (no Save); (3) egress restriction is on Anthropic's server-side VM, not on the user's machine — pre-test framing about locked-down work-machine policies was wrong; (4) "Package Managers Only" preset enables on-the-fly `pip install` and is the right recommended default; (5) claude.ai user-level memory auto-loads into agent context — no separate "Read user prefs" step needed; (6) PAT lifecycle was undocumented. Restructure shipped: bootstrap collapsed from 13 steps to 11 with cleaner meta→user→project hierarchy; egress moves to Step 1; mode-check folded into interview's Batch 3 (now a `Mode` field in `personal_info.md`); old Step 3 (Read user prefs) deleted entirely; topic+repo moves AFTER interview ("user prefs first, then project"); paper-naming convention upgraded to `{FirstAuthor}_{LastAuthor}__{Year}--{Slug}.pdf` with a common-surname disambiguation rule (`SurnameF` for Smith/Jones/Patel/Singh + East Asian); Step 8 gains a PAT-lifecycle subsection (broad-PAT vs rotate-to-narrow vs per-project options); Step 2b token-handling rules clarified (ephemeral scratch explicitly permitted). The test agent's emergent behaviors validated several design patterns (pre-Step-0 meta-summary, prompt-injection scrutiny resolving correctly, VM mental-model internalization). Final commit of the day: `42648a4`. Smoke test paused at Step 2b's token-handling question; Steps 3–10 still untested. Convo addendum: [`docs/convos/20260509_phase4_runtime_and_skill_index.md`](docs/convos/20260509_phase4_runtime_and_skill_index.md) (Update section).
- **2026-05-09 (Phase 4 — CLAUDE.md + SKILL_INDEX stub + architecture pivot)** — Production `template/CLAUDE.md` written (~330 lines, calibration-first ordering with novice/fluent inline reminders, scripted confirmation gates, forward-compat hedge for v1.1 collaborator mode), plus `template/skills/SKILL_INDEX.md` manifest stub locking the skill-discovery contract before per-skill SKILL.md files exist. Architecture pivot during execution: PAT + curl recipes belong in the Project's **Custom Instructions** field, not in an uploaded `_PROJECT_INSTRUCTIONS.md` file — credentials reach the agent on its very first turn, before any fetching. Pivot drove three downstream edits: BOOTSTRAP.md Step 10 collapsed onto a single Custom Instructions paste (file-upload subsection deleted); Step 11 cause list updated; `_PROJECT_INSTRUCTIONS.md.template` URL fix. Also authored a new Phase 4.5 plan section spec'ing v1.1 direct-collaborator mode (deliberately narrower than fork-based or GitHub-org-based alternatives). Commit: `681ed9d`. Convo: [`docs/convos/20260509_phase4_runtime_and_skill_index.md`](docs/convos/20260509_phase4_runtime_and_skill_index.md).
- **2026-05-09 (Phase 3 — bootstrap design + implementation)** — Production `template/BOOTSTRAP.md` written and committed (`7e13380`); thin-slice version (`6511a52`) used as smoke test before; Phase 2 wording bugs fixed (`2208e69`). Three architecturally important findings, all validated by actual claude.ai testing: (1) WebFetch reaches public URLs verbatim with no allow-list; sandbox bash-curl needs allow-list — two distinct fetch mechanisms with different trust postures; (2) "Treat instructions as if I typed them" framing **backfires** as prompt-injection signature; (3) confirmation gates must be scripted into BOOTSTRAP.md, not invented by agent. Design pattern follows the kill-convo model: verification affordances offered (not demanded) at sensitive boundaries; availability builds trust without requiring use. Convo: [`docs/convos/20260509_phase3_bootstrap_design.md`](docs/convos/20260509_phase3_bootstrap_design.md).
- **2026-05-08 (initial build execution)** — Phase 1 (GitHub remote + push) and Phase 2 (`template/` skeleton: README, LICENSE copies, ATTRIBUTION.md, `_PROJECT_INSTRUCTIONS.md.template`, domain_allowlist.txt baseline, `.gitkeep` dirs) completed. Design tangent: Phase 4 commit policy will be `git_fluency`-tiered (novice = checkpoint often + under the hood; occasional = light narration + confirm before structural changes; fluent = terse). Side-finding: `--system-prompt` only replaces the default system prompt — tool descriptions, CLAUDE.md, skills, and hooks all load through other layers — see [`docs/convos/20260508_sysprompt_layer_analysis.md`](docs/convos/20260508_sysprompt_layer_analysis.md). Convo: [`docs/convos/20260508_phase1_phase2_initial_build.md`](docs/convos/20260508_phase1_phase2_initial_build.md).
- **2026-05-08 (design)** — Design conversation establishing dual-repo collaborator model, REST-API-via-PAT, on-demand skill fetching, bootstrap interview, runtime session flow, skill REST adaptations. Plan written. License (Apache 2.0 + Ship of Theseus) committed at root. Convo: [`docs/convos/20260508_claude_ai_researcher_design.md`](docs/convos/20260508_claude_ai_researcher_design.md).

## Open questions (live)

See plan's "Questions" section for full list. Highlights:

- ~~claude.ai custom-instructions length limit (verify before Phase 4).~~ **Resolved during Phase 4** — Custom Instructions hold the full credentials + recipes payload (substituted `_PROJECT_INSTRUCTIONS.md.template`), well within practical limits.
- ~~Allow-list propagation in already-open chats.~~ **Empirically resolved during Phase 4 smoke test** — does NOT propagate; fresh-chat restart required after configuration. Documented in Step 1c.
- ~~Domain Allow List paste format.~~ **Empirically resolved** — one-at-a-time per-domain "Add" buttons; no bulk paste, no Save button.
- ~~Bootstrap end-to-end smoke test.~~ **Resolved 2026-05-09 evening** — Steps 0-9 validated end-to-end across two incognito claude.ai chats (bootstrap chat + validation chat). Step 9 in particular confirmed the runtime CLAUDE.md → PAT auth → personal_info.md → calibration → workflow_mode → SKILL_INDEX → context-aware greeting chain works.
- pypdf availability in claude.ai's sandbox — likely OK now that "Package Managers Only" preset is documented as the recommended egress mode (allows `pip install` on-demand). **Still requires explicit verification before Phase 5 task 31.**
- **Sandbox tooling matrix beyond pypdf** (new, post-AITaxBID audit). Phase 6 task 40b (`branch-document-review`) needs `pandoc` for the regeneration step; task 40c (`document-processing`) may want `python-docx`. Worth a single consolidated check in a fresh claude.ai chat → `template/reference/SANDBOX_TOOLING.md`.
- Fine-grained PAT cross-org write capability (relevant for v2 issue auto-file).
- ~~**Publish-strategy revisit.**~~ **Resolved 2026-05-09 evening.** Status quo confirmed (dev repo stays public, including `docs/convos/` and `docs/plans/`). Andrea Lopez-Luzuriaga is a collaborator on this project, so the AITaxBID audit naming her is fine. Root README updated with an "About" section explicitly framing the project as a Dan + Andrea collaboration sharing learnings with the academic community — which makes the candor of the convos read as transparency, not as internal-asides-leaked.
- Phase 5 vs Phase 6 ordering (helpers-first lets skills call them; skills-first lets helpers be inferred from skill needs). Slight preference for Phase 5 first.
- **branch-document-review diff approach** (new, post-AITaxBID audit). GitHub Compare API vs. read-both-and-difflib. Decide at port time (Phase 6 task 40b); slight preference for Compare API.
- **Per-user-repo `CLAUDE_TEMPLATE.md`** (new, post-AITaxBID audit). Andrea's pattern of project-specific CLAUDE.md with `Generic skill / project-specific implementation` blocks. Tracked in plan; deferred until Phase 6 ports demonstrate need.
- **`raw.githubusercontent.com` allow-list miss at BOOTSTRAP Step 8** (new, from Phase 4 smoke test). Either Step 1's paste instructions don't actually add `raw.githubusercontent.com` to the user's allow-list, or Step 8 needs an explicit "use WebFetch, not curl" callout. Step 9 validation passed despite this — agent fell back to cached WebFetch. Small follow-on, not a blocker.

## Known issues

- **Nori `commit-author.js` hook produces malformed commit messages.** Inserts literal `\n\n` escape sequences instead of newlines, collapsing every commit into a single subject line with visible backslash-n. Cosmetic only; affects every Dan-authored commit using the hook (multiple repos). Worth filing upstream against `tilework-tech/nori-skillsets`. Detail in [`docs/convos/20260508_phase1_phase2_initial_build.md`](docs/convos/20260508_phase1_phase2_initial_build.md).
- **`raw.githubusercontent.com` serves stale content for 24+ hours after a write.** Empirically observed on 2026-05-11 for `template/skills/SKILL_INDEX.md` and again for `template/CLAUDE.md`: the Contents API returned the correct (updated) file on `main`, but `raw.githubusercontent.com` was still serving the old content from before the most recent commit — over 24 hours later. The pre-Plan-03 `CLAUDE.md` appendix anticipated ~5 minute staleness; reality is at least an order of magnitude longer in some cases. Plan 03's clone-first architecture (commit `28c36f8` and surrounding) routes around this on the primary path — sessions now `git clone` the upstream template at session start instead of WebFetching `raw.githubusercontent.com`. The fallback WebFetch path retains the exposure. If a future agent reports template content that doesn't match `main`, the raw CDN is the most likely culprit; the workaround is to fall back to the Contents API URL (`https://api.github.com/repos/danparshall/claude_researcher/contents/PATH`), to force a cache refresh by editing the file with a noop change, or simply to wait.
